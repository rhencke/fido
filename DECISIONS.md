# Decisions

Every current human decision, accepted or open. **Rob alone decides.** One row per decision; edit the row
when it changes. Git history owns the previous row and the debate that produced it.

A settled decision reopens only through Rob, and only on new information: a spec fact, a proof obstruction,
an implementation fact, or a changed project goal.

## Accepted

| id | status | decision | reopen_trigger |
|---|---|---|---|
| ADR-0001 | ACCEPTED | Pinned target: Go 1.23 on `linux/amd64` with `GOAMD64=v1`. `int` and `uint` are 64-bit and DISTINCT from `int64`/`uint64`. There is no `TargetConfig`. No other OS or architecture is validated. `.review/scope.tsv` row `SR-001` records the boundary this decision draws and what enforces it. | C16, or any earlier request for another target or for `uintptr`. |
| ADR-0003 | ACCEPTED | Authority ordering: definite Go spec text owns meaning; the pinned `gc` owns target acceptance and adequacy where the spec permits latitude; `gc` never narrows required formal latitude. | New evidence of a definite spec/toolchain conflict. |
| LAT-X004 | ACCEPTED | Permitted rounding while computing untyped floating-point and complex constant expressions is settled as option (ii): the rounding-invariant accepted domain. | A measured proof cost or spec reading that defeats the invariant domain. |
| C6-SCOPE-ROWS | ACCEPTED | The three scope restrictions C6 builds on are accepted: `SR-007` imports on hold, `SR-008` ASCII-only source identifiers, and `SR-009` the bounded `Float.Decimal` literal domain. C6 depends on each directly — the closed world, `Names.OrdinaryIdentifier`, and `Float.NonNegativeDecimal` — so accepting C6 would otherwise have silently accepted three open proposals. `SR-009` accepts the *boundary*; `ADR-0002` still owns whether that exact bound is the right one. | Any of the three being challenged, or the milestone that lifts it. |
| SINGLE-FORMAL-AUTHORITY | ACCEPTED | Active milestones are formalized directly in their canonical Rocq owners; there is no separate executable specification, independently elaborated "contract surface", or shadow implementation. Future milestones stay prose-only until their dependencies are accepted, and a falsified load-bearing accepted fact triggers a targeted causal dependency retreat from the exact affected accepted contract — freeze its real dependents, repair the earliest affected guarantee, reopen only actual causal dependents, and give explicit negative causal closure for every checkpoint claimed unaffected, never a chronological replay. Governing prose owns meaning, scope and boundaries; canonical code owns exact formal realization; a mismatch is a defect. Normative details live in `ARCHITECTURE.md` §1 "Two authorities, never two formal implementations" (fixed point ARCH-13). | Evidence that developing a milestone's formalization directly in its canonical owner is untenable, or Rob changing the process. |
| STRICT-CHECKPOINT-SCOPE | ACCEPTED | Whole-system inspection is mandatory at every checkpoint, but a finding blocks the active checkpoint only when it violates that checkpoint's accepted semantic, provenance, public-guarantee, production, artifact, trust-boundary or required-dependency contract, or leaves a prerequisite the next work consumes unestablished. Tooling, gate-hardening, mutation-coverage, reporting, documentation, performance and repository-hygiene findings that do not undermine that foundation are mandatory concurrent work, assigned to the earliest checkpoint that must consume or close them, each with a named earliest mandatory closure point; discovery alone never expands the active checkpoint, and this is not permission to defer cleanup indefinitely. Concurrent treatment is never eligible for the hard blockers — a wrong public type/index/constructor topology; lost, reconstructed or ambiguous provenance; a theorem weaker or stronger than the accepted guarantee; a duplicate semantic authority; an axiom, `Admitted`, unimplemented assumption, trusted fallback, fuel, arbitrary bound or under-approximation presented as correctness; changed source meaning, compiler decision, diagnostic, emitted byte, runtime behavior, capability or trust boundary outside the active contract; a gate defect that makes an unestablished prerequisite appear established; or any defect in an accepted lower fact the next semantic root consumes. Normative statement in `ARCHITECTURE.md` §1 "Strict checkpoint scope"; indexed in `CLAUDE.md` rule 15. | Rob changing the review-scope process, or evidence that concurrent treatment let an unestablished prerequisite reach acceptance. |
| C6-ROOT3-RECUT | ACCEPTED | C6 Root 3 lands the first honest static vertical — source shape, exact occurrence, scope/object/binding foundation, one retained partial static phase, and the permanent `Compiled`/`Rejected`/`OutsideScope` outcome — with the `println`/conversion/literal fragment resolved through the binding path (the dedicated `Println`/`Convert` authorities deleted). Deferred to later C6 roots: type-equation solving/acyclicity, alias/defined-type meaning, declaration/initializer consumption, result plans/assignment, `StaticVariable` formation, package init ordering/cycles, unused-local analysis, application families beyond conversion+`println`, and all runtime. | A further causal expansion proving the cut not dependency-closed, or Rob changing the cut. |
| COMPILE-OWNS-GO-STATIC-SEMANTICS | ACCEPTED | `Compilable` owns every representable Go static accept/reject fact: name resolution and visibility, type identity and rules, constant typing/defaulting/conversion/representability, unary/application/statement/declaration legality, package rules, fresh-build preflight, exact diagnostics, exact unsupported requirements, and the one three-way `Compiled`/`Rejected`/`OutsideScope` verdict. `Typing`, binding and package facts are physical child units of this one authority, not peer semantic layers; there is no top-level `Typing`/`Bindings`/`Packages` authority and no peer `Admissible` stage — `Admissible` is a declarative characterization of the exact `Compiled` branch and mints nothing. `Compilable.compile` is the sole `Compilable.Program` mint. `Safe`, `Render`, `Emit`, materialize/sink never decide Go static validity; pinned `go build` is an external adequacy alarm, never Fido's semantic authority. | Rob changing the layer law, or a correct local rule that cannot live under `Compilable` without a genuine lower reusable abstraction. |
| RUNTIME-OVER-COMPILED-SAFETY-REFINES | ACCEPTED | Runtime and the concrete `Machine.T` are defined over the exact `Compilable.Program` (or a capability projecting it), describing both safe and unsafe compiled behavior. `Safe.certify` is proof-taking — `Safe.Program` retains the exact `Compilable.Program` plus a proof of `Safe.Property` over it — and adds only Fido-specific safety facts. Runtime never depends on a pre-existing `Safe` certificate, and `Safe` never defines a second behavior relation; this keeps race/deadlock/bad-prefix safety non-circular and non-vacuous. Accepted `Machine.T` is unchanged. | Rob changing the runtime/safety ownership, or evidence that a Runtime built over `Compilable.Program` cannot state the accepted `Safe` properties without changing accepted `Machine.T`. |

## Open

| id | status | decision | reopen_trigger |
|---|---|---|---|
| ADR-0002 | OPEN | The bounded `Float.Decimal` literal domain (`\|coeff\| < 10^40`, `\|exp10\| ≤ 4096`) stands as an explicit unresolved restriction. It carries no new correctness claim. | Before C7 accepts broader floating constants, or when measured proof cost justifies a replacement. |
| ADR-0004 | DEFERRED | The multi-platform 64-bit target set. No target beyond go1.23.2 linux/amd64 is covered. | C16. |
| TOOLCHAIN-PROVENANCE | OPEN | Replace the pending local-distribution evidence with verified official tarball bytes, or expressly accept `PASS-WITH-PENDING-PROVENANCE` for a specific use. Identities in `TOOLCHAIN.md` are exact; their chain to go.dev is not. | Any claim that depends on toolchain provenance rather than toolchain identity. |
| EXTERNAL-EVIDENCE | OPEN | Commit the external Go-distribution evidence components into a Git subtree, or expressly retain them as external `PROVENANCE-PENDING` references. Their bytes are not in Git, so their recorded hashes cannot be recomputed from the repository. | A requirement to recompute external evidence from the repository alone. |
| SCOPE-APPROVAL | OPEN | Accept or amend the five remaining `PROPOSED` rows in `.review/scope.tsv` (SR-002 … SR-006). Each states a live restriction that no current milestone depends on: unmodelled platform limits, the cooperating-developer threat boundary, buildx-only builds, the module-path grammar, and the source-file naming grammar. | Any of those restrictions being challenged, or a milestone that needs one lifted. |
| SOURCE-CHARSET | OPEN | Normalize tracked text to ASCII where a character is typographic, keeping UTF-8 only where it carries meaning. ASCII is keyboard- and grep-friendly for an English codebase; UTF-8 is not a defect. Load-bearing and must stay: verbatim pinned-spec quotations in `.review/latitude.tsv` (including `日` and `ø`), and the `✓` in gate output. | Rob deciding, or a normalization pass being folded into a milestone that already touches these files. |

# Go: every .go is generated, never checked in.  There is no emission this round; the uncommittable-Go seal
# + ocaml-origin gate are the backstops, and any generated .go stays ignored if emission returns.
*.go
/fido
*.test
*.out
vendor/

# Environment
.env

# Rocq build artifacts — .v sources are checked in, these are not
*.glob
*.vo
*.vok
*.vos
*.vio
*.aux
.*.aux
.lia.cache
.nia.cache
.coq-native/
_build/

# Go build output
/fido
*.test
*.out
vendor/

# Environment
.env

# Negtest harness scratch output (a negtest that wrongly EXTRACTS leaves this; run.sh cleans it)
/neg_out.go

# Rocq build artifacts — .v and .go sources are checked in, these are not
*.glob
*.vo
*.vok
*.vos
*.vio
*.aux
.*.aux
.lia.cache
.coq-native/
_build/

#!/usr/bin/env python3
"""generate_human_review_index — directive v12 [BUNDLE stub]. Implement at terminal execution to the exact schema and duties in
directive v12 (T-0 / T-18): see .review/spec-closure-campaign/directives/FIDO_TERMINAL_CORRECTNESS_DIRECTIVE.md
and tools/SELECTOR_SPEC.md for the selector algebra this must use.

THIS IS NOT THE LIVING-FCB GENERATOR. It is historical campaign provenance for the frozen terminal bundle,
and its schema is that bundle's provenance schema, not the living-FCB schema. Governance D-07 for the live
corpus is implemented by `tools/human-review-index.py` over
`.review/fcb/current/FIDO_FCB_HUMAN_ACTS.tsv`. Nothing in current code or documentation calls this file, and
nothing may route to it: there is no compatibility wrapper and its old schema must not be copied forward."""
raise SystemExit("stub: spec lives in directive v12; selector semantics in SELECTOR_SPEC.md")

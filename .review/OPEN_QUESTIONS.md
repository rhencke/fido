# Open questions — Claude Code to the reviewer and to Rob

None open.

The three questions this file carried were all about the Build Observatory: whether its residue set should
derive from `.gitignore`, whether it should run inside Buildx so the host needed no Python, and whether a
pre-commit stage's retained metric kind should be renamed. Rob withdrew that architecture, so their subjects
are gone rather than answered. What remained useful returned to M3 as terse findings in
`.review/M_SERIES_PLAN.md` — the policy-gate cost audit and the mutation-harness copying and execution
architecture.

Asking is not blocking. When implementation raises a scoping call or an ambiguity that is neither a contract
conflict nor a tracked human act, it belongs here rather than in a guess or a stall. Every entry names its
owner, whether it blocks, and the default taken if nobody answers — a question without a default is a blocker
invented for oneself. This file is not authority and overrides nothing.

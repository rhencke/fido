# M1 IMPLEMENTATION REVIEW — BLOCKING — REPAIR 5

## Disposition

**M1 implementation candidate `de6cf13f215e6c630fb267413315f584f675b264` is BLOCKING.**

It becomes the **fifth blocked M1 implementation candidate**.

Documentation freeze `eaafeb549aca7ae051632f0728c4be72f234b3d1` is not a separate implementation candidate.

**M1 repair 5 is the sole permitted implementation work.**

C4 and M0 remain accepted. This review does not reopen either checkpoint.

M2, M3, M4, C5 Step 0, C5, and all feature work remain forbidden.

No source, proof, gate, build, or semantic change is authorized. This is a small current-document completion repair
inside M1's accepted prose contract.

Use:

```text
/loop 3m
```

Continue until every required correction is complete or a real M1 contract conflict blocks progress. When complete
or genuinely blocked, notify Rob with the notification tool.

---

## 1. Exact review basis

```text
Uploaded snapshot: fido-main - 2026-07-28T182145.042.zip
Uploaded Git head: eaafeb549aca7ae051632f0728c4be72f234b3d1
Blocked M1 implementation candidate: de6cf13f215e6c630fb267413315f584f675b264
Documentation-only freeze: eaafeb549aca7ae051632f0728c4be72f234b3d1
M1 baseline authority ref: 068d3371ac3300303d6c7c99a97ed884182c81e4
Previous blocked M1 candidate: c73925c6b7c432a8265bdb052388efaf57d96f6f
M1 authority: .review/M1_SOURCE_DIET.md
Accepted review basis: .review/REVIEW_BASIS.md
```

Use all code, FCB, gates, ledgers, and documentation from this exact ref. Do not reset, rebase, rewrite history,
or mix refs.

Preserve `life.md` exactly.

---

## 2. What repair 4 closed

Keep every result below.

1. Every M1 obligation now names the command which actually runs its evidence.
2. `Makefile:diet` appears only on M1-04, M1-05, M1-06, and M1-11.
3. Exit-only evidence names the explicit M1 verifier.
4. M1-12 names both the permanent mutation run and the explicit `--m1` run.
5. M1-14 names both `make check` and the exact source-identity verifier.
6. The stale Makefile sentence about permanent file-disposition checking is gone.
7. No Make recipe, source, tool, hook, Docker stage, Dune file, OCaml file, generated byte, golden, or `life.md`
   byte changed.
8. Candidate-owned evidence remains immutable in the documentation-only freeze.
9. The exact baseline-to-candidate metrics reproduce.
10. Every surviving top-level Rocq command matches the baseline after removing exactly the eight ledgered
    declaration units.
11. The permanent source-comment gate remains independent of the temporary M1 baseline.

The implementation, evidence topology, and source diet now look complete. The remaining blocker is current prose
which directly violates M1's accepted rule that Git owns archaeology.

---

# 3. Blocking finding A — `NEXT_STEPS` copies the history it says Git owns

## 3.1 Contract violated

This violates:

- M1 purpose: Git owns archaeology;
- M1-09: duplicate history and state are removed from current operational documents;
- the file-purpose claim that `NEXT_STEPS` owns current checkpoint and candidate state;
- M1's rule to remove repair and candidate narrative from current documents.

## 3.2 Current defect

`NEXT_STEPS` correctly names the current repair-4 candidate, then devotes a long block to the refs, freezes, and
failure narratives of four earlier M1 candidates.

The block ends by saying Git owns those reviews and candidates, while copying the same history into the live
authority.

The prior refs and detailed findings do not direct current work. The current repair directive and Git history own
them.

## 3.3 Required replacement

Delete the entire block beginning:

```text
**THE FOUR EARLIER M1 candidates are BLOCKED.**
```

through its final historical sentence.

Replace it with exactly:

```markdown
- **Earlier M1 candidates are superseded.** Git history owns their refs, freezes, findings, and repair narratives.
```

Do not retain the old SHAs or summaries elsewhere.

In the repair-5 candidate, `NEXT_STEPS` names repair 5 as active and offers no candidate. In the later
documentation-only freeze, it names only the new repair-5 candidate and its current evidence state.

---

# 4. Blocking finding B — the current unsupported-boundary document carries repair archaeology

## 4.1 Contract violated

This violates:

- M1 purpose;
- M1-09;
- the whole-file current-purpose review;
- the rule that a current unsupported boundary states what is true now rather than how an earlier draft was
  corrected.

`.review/UNSUPPORTED_AND_RESTRICTED_SCOPE.md` is retained as the current unsupported-boundary authority. It is not
a frozen review record.

## 4.2 SR-005

Current text begins:

```text
Correction (this repair): SR-005 previously discussed only ...
```

Replace the complete introductory item with:

```markdown
- **Current exclusion:** The canonical `ModulePath.T` grammar excludes all of the following:
```

Keep the existing exclusion list and all current technical content after it. Do not preserve the correction
story.

## 4.3 SR-006

Current text begins:

```text
Correction (this repair): SR-006 previously claimed ...
```

Replace the introductory paragraph before the two lettered restrictions with:

```markdown
- **Current exclusion:** `FilePath.component_ok` combines two distinct restrictions:
```

Keep the existing `(a)` and `(b)` restriction descriptions. Remove the old-claim comparison and words whose only
purpose is to correct the former text.

The resulting section must state directly:

- the `cmd/go` selection exclusions; and
- the additional lowercase/alphanumeric Fido narrowing.

Do not weaken either restriction.

## 4.4 Float boundary

Current text says:

```text
No float implementation change is authorized by this C4 repair.
```

Replace it with:

```text
No float implementation change is authorized until ADR-0002 is resolved and an accepted checkpoint permits it.
```

Do not change ADR-0002's status or the float domain.

## 4.5 Search closure

Search every current, non-historical operational document for the exact phrases:

```text
Correction (this repair)
this C4 repair
previously claimed
previously discussed
```

After the specified changes, no such phrase may remain outside:

- an immutable accepted amendment record;
- the current repair directive;
- a test fixture which must contain the phrase to prove rejection.

Do not expand this into the nonblocking M3 fragile-prose audit.

---

# 5. Matrix and evidence corrections

Reopen M1-09 and M1-15 in the repair-5 candidate.

## 5.1 M1-09

After the current-document fixes exist, close M1-09 with exact positive-evidence tokens for:

```text
.review/NEXT_STEPS.md:Earlier M1 candidates are superseded.
.review/UNSUPPORTED_AND_RESTRICTED_SCOPE.md:Current exclusion:
.review/UNSUPPORTED_AND_RESTRICTED_SCOPE.md:No float implementation change is authorized until ADR-0002 is resolved
README.md:Emit.Mint.issue
```

The row may continue to use the existing FCB/reference gates for path and current-authority integrity. Human review
establishes that the prose is current and complete.

## 5.2 M1-15

Keep M1-15 open until the new exact candidate and documentation-only freeze exist. Close it only through the
existing freeze-overlay verifier.

## 5.3 Candidate-owned evidence

Update `.review/M1_FILE_DISPOSITION.tsv` for:

- deletion of `.review/M1_IMPLEMENTATION_REPAIR_4.md`;
- creation of `.review/M1_IMPLEMENTATION_REPAIR_5.md`;
- the exact new candidate sizes of changed files.

Keep `.review/M1_METRICS.tsv` header-only in the implementation candidate.

The documentation-only freeze may change only the already accepted closed overlay:

```text
.review/M1_METRICS.tsv
.review/M1_OBLIGATION_MATRIX.tsv
.review/NEXT_STEPS.md
.review/REVIEW_REQUEST.md
```

It must not alter candidate-owned evidence.

---

# 6. Strict-scope disposition

| Finding | Contract violated | Blocks M1 | Mandatory owner |
|---|---|---:|---|
| `NEXT_STEPS` repeats earlier M1 candidate and repair history | M1 purpose and M1-09 | **yes** | M1 repair 5 |
| Current unsupported-boundary prose narrates prior repairs and claims | M1 purpose and M1-09 | **yes** | M1 repair 5 |
| Stable-prose identities versus copied counts and positions | none required for M1 acceptance | no | `M3-FRAGILE-PROSE` |
| Claim-matrix call-graph hardening | none required for M1 acceptance | no | M3 |
| Build timing and dependency cost | none in M1 | no | M2 |
| Proof-module and build-graph restructuring | none in M1 | no | M4 |
| `life.md` content | none; separately authorized character continuity | no | preserve exactly |

Do not pull any nonblocking work into repair 5.

---

# 7. Allowed changes

Repair 5 may change only:

```text
.review/NEXT_STEPS.md
.review/UNSUPPORTED_AND_RESTRICTED_SCOPE.md
.review/M1_FILE_DISPOSITION.tsv
.review/M1_OBLIGATION_MATRIX.tsv
.review/M1_IMPLEMENTATION_REPAIR_4.md       # delete
.review/M1_IMPLEMENTATION_REPAIR_5.md       # create
.review/fcb/current/FIDO_FCB_REFERENCES.tsv
.review/REVIEW_REQUEST.md                   # current state, then freeze
.review/M1_METRICS.tsv                      # header in candidate, exact table in freeze
```

A generated FCB view may change only if an existing gate proves that one of the exact reference-row edits requires
it. No such change is expected.

Do not change:

- any `.v` file or comment;
- any source-diet tool;
- any other tool;
- Make, Docker, Dune, hooks, OCaml, plugin, fixtures, or goldens;
- README, CLAUDE, ARCHITECTURE, PROGRESS, PAINFUL_LESSONS, or `life.md`;
- the M1 contract or accepted review basis;
- any FCB policy or semantic contract;
- generated Go.

No new FCB amendment is required.

---

# 8. Verification

Run the unchanged gates:

```text
make diet
make fcb
make claims
make names
make check
make regenerate
make regen-guard
make fmt
make audit-fresh
```

Run the real staged pre-commit hook without bypassing it.

Run the explicit M1 exit evidence:

```text
python3 tools/source-diet.py --m1-self-test
python3 tools/gate-mutation-test.py --m1
python3 tools/source-diet.py --verify-m1-evidence \
  --baseline-ref 068d3371ac3300303d6c7c99a97ed884182c81e4 \
  --candidate-ref <new repair-5 candidate>
```

Also verify:

1. the only current M1 candidate SHA is the new one in the freeze's current-state files;
2. no earlier M1 candidate SHA or failure summary remains in `NEXT_STEPS`;
3. the three stale unsupported-boundary phrases are absent as required by §4.5;
4. every current exclusion and ADR status is unchanged in meaning;
5. every surviving top-level Rocq command remains identical;
6. generated bytes and runtime results remain unchanged;
7. candidate-owned evidence is unchanged by the freeze;
8. M2 through M4 and C5 remain forbidden.

Do not create a prose-heavy closure audit.

The prior review independently established the permanent gates, exact source identity, generated hashes, and runtime
goldens. Repair 5 changes none of those implementation paths, but the complete project verification must still run
before the new freeze.

---

# 9. Definition of done

Repair 5 is complete only when:

- `NEXT_STEPS` carries current M1 state and no copied candidate history;
- SR-005 and SR-006 state their current exclusions directly;
- the float boundary no longer refers to a C4 repair;
- M1-09 closes with exact current-document evidence;
- candidate-owned evidence is exact;
- every unchanged gate passes;
- one new implementation candidate is committed;
- one later documentation-only freeze names it;
- no commit follows the freeze;
- Claude notifies Rob.

Only Rob accepts M1.

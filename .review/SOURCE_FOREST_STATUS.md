# Source Forest Campaign — Status Ledger

Campaign: **Specification-Shaped Source Forest, Snapshot-Local Occurrence Identity, and Occurrence-Anchored
Compilation.** The full design is `.review/SOURCE_FOREST_MASTER_PLAN.md`; Git history is the detailed archive.

## Authority

- Active checkpoint: **C3**.
- Functional contract: `.review/C3_FRESH_IMAGE_LITERAL_BUILD_PLAN.md`.
- Contract SHA-256: `a13779c2e55c679e461e857d019eeae6adef27b0666876ed0cac92833814f212`.
- Accepted C3 basis: `.review/REVIEW_BASIS.md`.
- Current state: substantive code, proofs, gate, fresh-build runner, publication workflow, generated bytes, and
  size closeout are complete; a final human-directed prose repair is pending approval.
- C4 and every later checkpoint remain forbidden.

## Completed checkpoints

| Checkpoint | Scope | Result |
|---|---|---|
| C0 / C0A / C0B | occurrence-index proof spike, snapshot-local identity, exact source correspondence | GREEN, human-approved |
| C1 / C1A / C1B | specification-shaped files, path-keyed source forest, standard collections | GREEN, human-approved |
| C2 | production occurrence index, references, navigation, indexed traversal | GREEN, human-approved |

## C3 implementation state

The current candidate has:

- one retained indexed elaboration;
- occurrence-keyed facts and structured diagnostics;
- factored package declaration and command-entry rules;
- exact fresh-image modeling of pinned `go build ./...`;
- component-based package import paths and executable naming;
- fail-closed fresh-run classification and fault tests;
- no manifest or checksum attestation mechanism;
- `Fido Materialize` as the public certified export;
- supported publication ordered through the validated Docker dependency graph;
- readable assumptions gate at 386/386;
- zero project assumptions;
- byte-identical generated `go.mod` and `.go` files;
- no C4 work;
- a repository more than 400 KB smaller than the pre-weedwhacker snapshot.

The last bounded confirmation blocked only on current prose: a C3 Contract Review exception missing from the
master plan, malformed `GoCompile.v` comment headings, and obsolete historical review text in the master plan and
collection audit. Rob's manual prose-only patch (payload sha256 `b9034155…d7d1681`) was applied to close those
residues; it touches only comments, Markdown/authority prose, and the deletion of the superseded semantic-prose
directive. Verified from this candidate: `make prove` (readable gate **386/386**, whole-theory audit, self-tests
A-E), `make e2e`, `make check`, `make regenerate`, `make regen-guard`, staged pre-commit — all GREEN; generated
`go.mod` + recursive `.go` byte-IDENTICAL; `git diff --check` clean; `GoCompile.v` differs in COMMENTS ONLY (no
Gallina/theorem/proof change); the frozen contract sha256 is unchanged; the repository is smaller than before
the closeout. This snapshot contains no authorization for another autonomous review loop. **Human C3 approval is
pending** after the prose repair is inspected.

## Standing decisions

- Platform resource limits such as NAME_MAX, PATH_MAX, disk, and memory are outside the semantic model.
- Contract Review precedes implementation for checkpoints activated after the policy was adopted. C3 is the
  explicit transition exception: its accepted manual basis replaces a retroactive Contract Review.
- Each review permits at most one bounded confirmation. A blocking confirmation returns control to Rob.

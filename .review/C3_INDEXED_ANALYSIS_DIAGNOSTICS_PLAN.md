Claude Code directive: C3 — one retained indexed analysis, occurrence-keyed facts, and structured diagnostics

Repository:
  rhencke/fido

Baseline:
  a812812d0ae311471c672bde0bbbcea057135ca6
  review(final): C2 — record FINAL Codex GREEN disposition (awaiting Rob's approval)

HUMAN DISPOSITION:
  Rob has now reviewed and APPROVED C2 by issuing this C3 activation directive.

THIS IS THE ONE ACTIVE CHECKPOINT.

C2 is accepted.  C3 is authorized.  C4 and every later Source Forest checkpoint remain FORBIDDEN until C3 is
Codex-green, pushed, and explicitly approved by Rob.

Before implementation, write this directive VERBATIM to both:

  .review/C3_INDEXED_ANALYSIS_DIAGNOSTICS_PLAN.md
  .review/NEXT_STEPS.md

Update:

  .review/SOURCE_FOREST_STATUS.md

to record:

- C2 human disposition: APPROVED by Rob;
- C3 active checkpoint;
- baseline SHA above;
- C3's binding outcome: one retained IndexedProgram per analysis, one analysis execution root, occurrence-keyed
  successful facts, structured diagnostics anchored in the exact rejected snapshot, and `go_compile` as a
  projection of analysis;
- C4 remains forbidden;
- this directive is the binding completion contract.

Commit activation before implementation:

  milestone(contract): C3 — activate retained indexed analysis and structured diagnostics

Then run exactly:

  /loop 5m completing NEXT_STEPS until codex review is green and then notify me and stop the loop.

C3 uses TWO intentional Codex barriers:

  ROOT  — the retained indexed-analysis root, fact tables, diagnostic core, and exactness theorems;
  FINAL — deterministic reporting, downstream integration, residue removal, e2e, docs, and byte identity.

There is no human stop between ROOT GREEN and FINAL.  After ROOT GREEN, continue directly to FINAL.

===============================================================================
0. EXECUTIVE OUTCOME
===============================================================================

At C3 completion, the production pipeline is structurally:

  GoProgram p
      |
      | exactly once
      v
  IndexedProgram p
      - the original immutable GoProgram remains the source authority
      - one retained Snap.SyntaxIndex p is the structural navigation authority
      |
      | one production semantic execution root
      v
  analyze_indexed
      |
      +-- AnalysisOK (CompilationFacts p ip)
      |       - exact expression facts keyed by canonical occurrence identity
      |       - exact package / entry facts
      |       - proof of the existing declarative GoCompile judgment
      |
      +-- AnalysisFailed nonempty structured diagnostics
              - every primary/related anchor belongs to the exact rejected p
              - no successful CompilationFacts escape
              - canonical deterministic order

The public convenience flow is:

  analyze p
      let ip := index_program p in
      analyze_indexed p ip

  go_compile p
      projection of analyze p
      NEVER an independent validity computation

  CompilableProgram
      retains:
        - original GoProgram
        - the exact retained IndexedProgram
        - CompilationFacts

  SafeProgram / GoRender / GoEmit
      continue rendering the ORIGINAL source program
      NEVER render from the index, facts, diagnostics, or a rewritten tree

C3 adds NO Go language feature and changes NO generated byte.

===============================================================================
1. STANDING LAWS
===============================================================================

Preserve every accepted standing law:

1. Zero project axioms.
2. No `Admitted`.
3. No kernel primitives forbidden by the project trust boundary.
4. No fuel.
5. One raw, specification-shaped AST.
6. No typed AST.
7. No resolved AST.
8. No copied compiler tree.
9. No parent links in source syntax.
10. No author-assigned node identities.
11. One standard-collection law:
    - identity keyed -> mature standard map;
    - membership only -> mature standard set;
    - ordered source/execution -> list;
    - duplicate-preserving bucket -> list as a map VALUE;
    - derived `elements` lists are enumerations, not authorities.
12. Indexing is structural, source-derived, total, and semantic-free.
13. Compilation owns semantic decoration, diagnostics, and whole-program acceptance.
14. GoTypes owns semantic type/constant relations, not source identity or diagnostics.
15. GoRender renders the original AST.
16. Generated bytes remain identical unless a separately reviewed rendering change explicitly authorizes drift.
17. Every public theorem surface remains assumption-free under the readable gate and whole-theory audit.
18. Failed structural/semantic construction never silently defaults to empty/success.
19. Standard-map overwrite must never erase duplicate source evidence.
20. C4 and later features are forbidden during C3.

Add this C3 law:

> One immutable source snapshot gets one retained structural index and one production analysis execution.
> Semantic facts and diagnostics decorate occurrence identity; they never replace, copy, or rewrite source syntax.

===============================================================================
2. CURRENT C2 BASELINE — FREEZE IT
===============================================================================

Do not reopen these accepted C2 decisions:

- `GoIndex.v` is the one structural occurrence authority.
- `Snap.SyntaxIndex p` is indexed by the exact immutable `GoProgram p`.
- outer index:
    standard FMapAVL FilePath -> FileIndex
- per-file index:
    standard FMapPositive positive -> NodeMeta
- root local id:
    1%positive
- `NodeKey`:
    FilePath + positive local id
- `FileRef p`:
    path + exact source + source-map membership
- `NodeRef p`:
    FileRef + local id + validity
- kind-refined refs:
    FileNodeRef / PackageClauseRef / DeclRef / StmtRef / ExprRef
- raw constructors remain sealed.
- parent lookup is metadata lookup.
- direct children use interval jumps.
- ancestry uses preorder intervals.
- `visit_file` runs the single-pass `walk_file`.
- each visited item is:
    validated NodeRef p + ORIGINAL SourceOccurrence
- no per-node source recovery on the hot path.
- no per-node index rebuild.
- `OccurrenceSpike.v` remains deleted.
- `GoIndex` imports no semantic compiler/type module.
- all source/index exactness and snapshot-locality theorem families survive.

C3 consumes these foundations.  It does not redesign them.

===============================================================================
3. RESPONSIBILITY AND DEPENDENCY DIRECTION
===============================================================================

The intended dependency direction after C3 is:

  GoAST
     |\
     | \
     v  v
  GoIndex    GoTypes
       \      /
        \    /
         v  v
        GoCompile
            |
            v
          GoSafe
            |
            v
       GoRender / GoEmit

Required ownership:

GoAST:
- owns source syntax and source maps;
- knows no index, facts, or diagnostics.

GoIndex:
- owns occurrence identity, references, navigation, and IndexedProgram construction;
- knows no semantic type, compiler acceptance, facts, or diagnostics.

GoTypes:
- owns exact constants, typed constants, conversion, representability, defaulting, and semantic typing relations;
- knows no diagnostic anchor;
- should not remain the production owner of indexed whole-program traversal.

GoCompile:
- is the sole layer where GoIndex identity and GoTypes semantics meet;
- owns IndexedProgram consumption, analysis, package grouping, CompilationFacts, diagnostics, and exact
  whole-program acceptance.

GoSafe:
- consumes CompilableProgram;
- adds safety evidence over the same retained source/index/facts.

GoRender / GoEmit:
- traverse the original source program projected from SafeProgram;
- do not inspect semantic fact maps to choose source spelling.

C2's `indexed_program_typedb` in GoTypes was a deliberate minimal integration demonstration.  C3 must subsume it
into the real analysis root.  Once the new exactness proof is complete:

- delete `indexed_program_typedb` and its aggregate helper surface, or move only indispensable theorem bridges
  into GoCompile;
- remove the `GoIndex` import from GoTypes if no genuine GoTypes-owned use remains;
- do not leave a permanent peer "indexed checker" beside `analyze`.

The declarative `ResolveExpr`, `ProgramTyped`, and the semantic leaf functions remain GoTypes authorities.

===============================================================================
4. INDEXED PROGRAM — THE MANDATORY PHASE BOUNDARY
===============================================================================

Add the explicit retained phase boundary in GoIndex.

Preferred shape:

  Record IndexedProgram (p : GoProgram) : Type := mkIndexedProgram {
    ip_index : Snap.SyntaxIndex p
  }.

  Definition index_program (p : GoProgram) : IndexedProgram p :=
    mkIndexedProgram p (Snap.index_program p).

A parameterized wrapper is preferred over copying `p` into a second field:

- the exact program is the type parameter;
- the one retained index is the field;
- no duplicated source snapshot;
- no equality transport between an internal program field and an external program argument.

If a different but propositionally equivalent shape is materially easier in Rocq, it must still guarantee:

- exact source snapshot indexing;
- one retained `SyntaxIndex p`;
- no second source copy;
- no arbitrary index constructor;
- no semantic field;
- one canonical `index_program`.

Expose:

  indexed_syntax :
    IndexedProgram p -> Snap.SyntaxIndex p

Prove/gate:

- `index_program` uses exactly `Snap.index_program p`;
- all GoIndex queries work through the retained `indexed_syntax`;
- no query reconstructs `Snap.index_program p`;
- the wrapper is axiom-free;
- the wrapper contains no source-tree copy.

Do not add source spans in C3.

===============================================================================
5. STANDARD NODEKEY MAP — FACT/DIAGNOSTIC STORAGE
===============================================================================

C3 needs mature, canonical maps keyed by occurrence identity.

Use the pinned Rocq standard library's FMapAVL over `GoIndex.NodeKey`.

Do NOT create a custom:
- association list;
- nested hand-written dictionary;
- trie;
- hash;
- sorting structure.

Define a thin ordered-key wrapper after NodeKey exists, preferably in GoIndex or one small structural
collection module imported by GoCompile:

  Module NodeKey_OT <: OrderedType.
    t  := NodeKey
    eq := Leibniz equality
    lt := lexicographic:
            FilePath order first
            positive numeric/local-id order second
  End NodeKey_OT.

  Module NodeKeyMapBase  := FMapAVL.Make NodeKey_OT.
  Module NodeKeyMapFacts := FMapFacts.WFacts_fun NodeKey_OT NodeKeyMapBase.
  Module NodeKeyMapProps := FMapFacts.WProperties_fun NodeKey_OT NodeKeyMapBase.
  Module NodeKeyMapOrd   := FMapFacts.OrdProperties NodeKeyMapBase.

Use the existing standard `FilePath_OT` and a pinned standard ordered positive key where possible.

Prove/gate:

- compare equality iff Leibniz NodeKey equality;
- NodeKey path/local lexicographic order;
- add/find same;
- add/find other;
- elements are key-sorted;
- elements have unique keys;
- extensionally equal maps have equal canonical elements lists;
- zero assumption closure.

The canonical NodeKey order is:

  path order from FilePath_OT
  then local positive order

This is the permanent source-occurrence order used by:
- fact enumeration;
- node-diagnostic enumeration;
- deterministic reports.

A domain wrapper may expose:
- empty;
- find;
- mem;
- add;
- MapsTo;
- Equal;
- elements;
- fold.

It must delegate all storage/collection operations to FMapAVL.

===============================================================================
6. REFACTOR THE SEMANTIC LEAF AUTHORITY FOR ONE-PASS ANALYSIS
===============================================================================

C3 must not compute `const_info` recursively once for every expression occurrence.  That would make a deep
conversion chain quadratic when producing facts for every node.

Refactor GoTypes so the ONE existing semantic authority has a reusable one-node layer.

Conceptually:

  const_info_step :
    GoExpr ->
    option ConstInfo   (* the one child status for a current conversion; None for a leaf *)
    ->
    option ConstInfo

Current meaning:

- literal leaves construct exactly the current CIUntyped value;
- an integer/float/complex conversion consumes its one child's ConstInfo and calls the SAME `convert_const`;
- child failure remains failure;
- no duplicated conversion/range/rounding logic.

Then define/prove the recursive source helper through that same step:

  const_info e
    =
  const_info_step e (status of its current expression child, if any)

Exact implementation shape may use a more strongly typed current-grammar child input, but do not add speculative
future child algebras.

Required:

- `convert_const` remains the sole conversion authority;
- `default_const` / `resolve_const_info` remain the sole default/use-resolution authorities;
- `const_info` and the C3 bottom-up analysis use the SAME step;
- no second rounding;
- no second representability relation;
- no second conversion classifier that can disagree;
- all existing numeric scars remain unchanged.

Gate the step/reflection theorem.

===============================================================================
7. PACKAGE REFERENCES — VALID ABSENCE ANCHORS
===============================================================================

A package spans files and is not one AST node.  Missing-main diagnostics therefore use a validated package
anchor, not a fake source node.

Define a thin proof-backed package reference in GoCompile:

  Record PackageRef (p : GoProgram) : Type := mkPackageRef {
    package_ref_key : string;
    package_ref_present :
      exists path sf,
        GoAST.maps_to_file path sf (prog_files p)
        /\ fp_parent path = package_ref_key
  }.

Equivalent formulation is acceptable.

Properties:

- package identity is the current package key: parent directory string;
- a PackageRef cannot name a package with no represented file;
- equality is key equality (proof fields are irrelevant);
- package anchors are snapshot-local through `p`;
- no package source node is invented.

Provide:

- package_ref_key;
- package_ref_eq_dec;
- package_ref_key_inj;
- package ref construction from a real FileRef/file binding;
- canonical package refs from PackageMap elements.

Do not use an unvalidated raw string as a public package diagnostic anchor.

===============================================================================
8. STRUCTURED DIAGNOSTIC CORE
===============================================================================

Land in GoCompile:

  DiagnosticAnchor p :=
    AtNode    (Snap.NodeRef p)
    AtFile    (Snap.FileRef p)
    AtPackage (PackageRef p)
    AtProgram

All anchor constructors are exact-snapshot handles.

Current C3 diagnostic reasons are exactly:

1. Invalid explicit conversion.
2. Default constant not representable.
3. Duplicate main declaration.
4. Package with no main declaration.

Use an intrinsically well-shaped reason type or equivalent constructors so invalid anchor/category combinations
are difficult or impossible.

Preferred conceptual form:

  Inductive DiagnosticReason (p : GoProgram) :=
  | DRInvalidConversion
      (primary : ExprRef p)
      (outer_context : list (ExprRef p))
      (target : GoType)
      (operand_status : ConstInfo)

  | DRDefaultNotRepresentable
      (primary : ExprRef p)
      (exact_constant : GoConst)
      (default_target : GoType)

  | DRDuplicateMain
      (later_primary : DeclRef p)
      (earlier_related : DeclRef p)

  | DRMissingMain
      (package_primary : PackageRef p).

Then derive:

  DiagnosticCode :=
    DCInvalidConversion
    DCDefaultNotRepresentable
    DCDuplicateMain
    DCMissingMain

  diagnostic_code
  diagnostic_primary : DiagnosticAnchor p
  diagnostic_related : list (DiagnosticAnchor p)

For nested invalid conversions:

- primary = the innermost conversion occurrence whose own conversion step fails;
- outer_context = enclosing conversion ExprRefs, nearest first;
- the outer println/use occurrence may be included only when useful and not identical to the primary;
- do not emit one duplicate diagnostic per ancestor.

For duplicate main:

- process main declarations in canonical NodeKey order inside the package;
- the first main is the baseline;
- every later main receives one duplicate diagnostic:
    primary = later declaration
    related = first declaration
- this yields n-1 deterministic diagnostics for n > 1 mains.

For no main:

- primary = PackageRef;
- do not invent a declaration/file node.

The diagnostic proof core carries:
- codes;
- valid anchors;
- structured values.

It does NOT carry authoritative English prose.

A pure report/formatting projection may later produce readable text.

No source spans, line numbers, or columns in C3.

===============================================================================
9. DIAGNOSTIC SOUNDNESS
===============================================================================

Prove code-specific soundness.

Invalid conversion diagnostic:

- primary ref denotes an explicit conversion expression;
- target is exactly that conversion's current target;
- operand_status is exactly the child expression's successful ConstInfo;
- the shared `const_info_step` / `convert_const` for that target returns None;
- every related outer ref is an actual strict ancestor conversion in the same file;
- related order is deterministic and contains no duplicate ref.

Default-not-representable diagnostic:

- primary ref denotes a println-use expression;
- its exact `const_info` is `CIUntyped c`;
- its default target is the current Go default:
    bool -> bool
    integer -> int
    float -> float64
    complex -> complex128
    string -> string
- `default_const c = None`;
- no explicit conversion node is falsely labeled as a defaulting error.

Duplicate-main diagnostic:

- both refs denote DMain declarations;
- they are distinct;
- both belong to the same PackageRef/key;
- related declaration precedes primary in canonical NodeKey order.

Missing-main diagnostic:

- PackageRef is present in p;
- the package contains no DMain occurrence.

General:

- every primary/related node ref belongs to p;
- every file ref belongs to p;
- every package ref denotes a represented package;
- no diagnostic identity is copied source syntax.

===============================================================================
10. OCCURRENCE-KEYED EXPRESSION FACTS
===============================================================================

Do not add separate type and constant maps that can disagree.

Use ONE expression fact value:

  Record ExprFact : Type := mkExprFact {
    ef_const_status : ConstInfo;
    ef_use_resolved : option ResolvedConst
  }.

Meaning:

- every expression occurrence in a successful program has exactly one `ef_const_status`;
- it is exactly `const_info` of that expression;
- `ef_use_resolved = Some rc` exactly for current use-context expressions (`RPrintlnArg _`);
- for a println argument, `rc` is exactly `resolve_expr_const UsePrintlnArg e`;
- conversion-operand/internal expressions have `ef_use_resolved = None`;
- type and resolved exact value are projections from the one ResolvedConst:
    resolved_type_at
    resolved_constant_at
- do not store a parallel GoType map.

Store facts in the standard NodeKeyMap:

  NodeKeyMapBase.t ExprFact

Wrap it in a proof-bearing domain abstraction indexed by the exact p and retained ip:

  ExprFactTable p ip

Required table laws:

- every map key mints a valid NodeRef p through ip;
- every key refines to ExprRef p;
- every expression occurrence has exactly one entry on success;
- no non-expression key has an entry;
- repeated equal syntax occurrences have distinct entries because their NodeKeys differ;
- no map add overwrites:
    insertion freshness follows from visit_file NoDup and is proved;
- canonical elements are source ordered.

Expose the public query through typed references, not raw keys:

  expr_fact_at :
    CompilationFacts p ip ->
    ExprRef p ->
    ExprFact

This query should be total on successful facts.

Raw-key lookup may remain an internal/proof API.

Facts store semantic values only.  They never store:
- GoExpr;
- SourceOccurrence;
- SyntaxView;
- parent/path duplicates;
- a rewritten expression.

===============================================================================
11. PACKAGE / ENTRY FACTS
===============================================================================

During analysis, collect a standard PackageMap:

  PackageMapBase.t (list (DeclRef p))

The list is a duplicate-preserving ordered bucket VALUE, not collection storage.

Required construction:

- initialize a package entry when the first file in that package is visited, even when the file has no main;
- collect DMain DeclRefs in canonical:
    FileMap path order
    then local NodeKey order;
- use efficient reversed accumulation + one final reverse, or another linear ordered-bucket method;
- do not use repeated append creating quadratic buckets;
- do not overwrite declarations.

On successful analysis, expose:

  Record PackageFact (p : GoProgram) := mkPackageFact {
    pf_main : DeclRef p
  }.

  PackageMapBase.t (PackageFact p)

Exactness:

- map domain is exactly the represented package set;
- each package fact's main ref denotes DMain;
- the main belongs to that package;
- each package has exactly one main;
- every source package has one fact;
- no package fact is invented.

Expose:

  package_main_at :
    CompilationFacts p ip ->
    PackageRef p ->
    DeclRef p

Total on success.

The existing `package_summaries` / count theorems may remain as declarative specification/proof helpers, but:

- production `analyze` must not independently run the old count checker as a peer authority;
- prove the new reference collection's lengths/counts agree with the existing declarative package judgment;
- do not keep two production package decisions.

===============================================================================
12. COMPILATION FACTS
===============================================================================

Define:

  Record CompilationFacts
    (p : GoProgram)
    (ip : IndexedProgram p) : Type := mkCompilationFacts {
      cf_expr_facts    : ExprFactTable p ip;
      cf_package_facts : PackageMapBase.t (PackageFact p);

      cf_expr_exact     : ...;
      cf_package_exact  : ...;
      cf_valid          : GoCompile p
    }.

Equivalent factoring is acceptable.

Do not add speculative fields for:

- names;
- resolution;
- methods;
- interfaces;
- imports;
- source spans;
- arbitrary declaration tables;
- future type syntax.

Only current live facts land.

CompilationFacts is evidence over:
- p;
- ip.

It is not:
- a new AST;
- a syntax copy;
- a renderer input;
- a second type universe.

===============================================================================
13. ONE ANALYSIS RESULT
===============================================================================

Define the permanent indexed result:

  Inductive AnalysisResult
    (p : GoProgram)
    (ip : IndexedProgram p) : Type :=
  | AnalysisOK
      (facts : CompilationFacts p ip)
  | AnalysisFailed
      (diagnostics : list (DiagnosticReason p))
      (diagnostics_nonempty : diagnostics <> []).

No custom nonempty collection implementation is needed; an ordered list plus a proof is correct because report
order is semantic/presentational and duplicates, if any, are explicit.

Then retain the index in the public analysis snapshot:

  Record ProgramAnalysis (p : GoProgram) : Type := mkProgramAnalysis {
    pa_indexed : IndexedProgram p;
    pa_result  : AnalysisResult p pa_indexed
  }.

Public root:

  analyze : forall p : GoProgram, ProgramAnalysis p

Implementation:

  Definition analyze p :=
    let ip := GoIndex.index_program p in
    mkProgramAnalysis p ip (analyze_indexed p ip).

`analyze_indexed` is the internal worker.

There is ONE production root:
- `analyze`.

No independently callable peer:
- indexed analyzer;
- old prog_ok compiler;
- legacy checker;
may remain as a production authority.

===============================================================================
14. ANALYSIS ALGORITHM — ONE INDEX, NO SOURCE RECOVERY, NO SUBTREE RESCAN
===============================================================================

The executable analysis must visibly do:

1. Build `IndexedProgram p` exactly once.

2. Enumerate source files through canonical `FileMap.elements`.

3. For each file:
   - mint one FileRef;
   - call `Snap.visit_file` once;
   - retain/use the delivered `(NodeRef, SourceOccurrence)` stream;
   - do not call `source_occurrence_of_ref` or `node_at` on the hot path;
   - use `indexed_syntax ip` for role/kind/children metadata lookup.

4. Analyze expression occurrences bottom-up without recursively recomputing every subtree.

Preferred current implementation:

- `Snap.visit_file` is preorder;
- use `fold_right` over the visited list (or one equivalent reverse-preorder pass);
- a child expression's outcome is in the temporary standard NodeKey map when its parent conversion is processed;
- read direct child refs through exact `Snap.children_of`;
- use source syntax from the delivered SourceOccurrence;
- use role/kind from the retained index through the NodeRef;
- use `const_info_step` for the semantic node decision.

5. Build expression facts/failures using standard NodeKey maps.

6. Build package main buckets using the same canonical visited stream and standard PackageMap.

Bounded linear passes over the already produced `visit_file` list are acceptable.

Forbidden:

- rebuilding index per file/query;
- source search by NodeRef;
- calling recursive `const_info e` independently for every expression occurrence;
- recomputing subtree ends;
- scanning all files once per package;
- structural equality dedup;
- copied located AST storage.

===============================================================================
15. EXPRESSION OUTCOME AND FAILURE PROPAGATION
===============================================================================

Use an internal current-grammar outcome concept:

  ExprOutcome p :=
    ExprOK ConstInfo
    |
    ExprFailed ExprFailure

  ExprFailure :=
    primary ExprRef p
    outer_context list ExprRef p
    reason payload

This is internal semantic state, not a collection implementation.

Rules:

Leaf:
- produce the exact current CIUntyped status.

Conversion with failed child:
- preserve the child's primary;
- append/prepend the current conversion ref to deterministic outer_context;
- do not emit a second diagnostic.

Conversion with successful child:
- call the shared `const_info_step`;
- success -> current ConstInfo;
- None -> new failure whose primary is the current conversion ref.

Println argument root:
- if expression outcome failed:
    emit its one diagnostic;
- if expression status succeeds:
    call `resolve_const_info`;
    Some -> record `ef_use_resolved`;
    None -> emit `DRDefaultNotRepresentable` at that argument ref.

Facts for successful child expressions may exist in internal temporary state while another sibling fails, but:

- an `AnalysisFailed` result exposes NO CompilationFacts;
- partial facts do not escape.

Prove innermost-failure localization for nested conversions.

Required scar:

  outer(inner(bad))

where:
- inner itself fails;
- outer would otherwise propagate;
- diagnostic primary is inner;
- outer is related;
- exactly one diagnostic is emitted for that argument.

===============================================================================
16. DIAGNOSTIC STORAGE AND CANONICAL ORDER
===============================================================================

Use standard maps internally.

Node-primary diagnostics:

  NodeKeyMapBase.t (list (DiagnosticReason p))

The list value is a code-ordered bucket.  Current C3 should normally create singleton buckets, but using a bucket
prevents future map overwrite from erasing multiple diagnostics at one occurrence.

File-primary diagnostics, if any current case genuinely needs one:

  FileMapBase.t (list ...)

Package-primary diagnostics:

  PackageMapBase.t (list ...)

Program-primary diagnostics:

  list ...

Canonical final flattening order is:

1. node diagnostics:
   - NodeKeyMap canonical elements
   - path then local id
   - finite DiagnosticCode order inside one node bucket

2. file diagnostics:
   - FileMap path order
   - code order inside bucket

3. package diagnostics:
   - PackageMap key order
   - code order inside bucket

4. program diagnostics:
   - finite code order

This order is explicit and stable.

No project-authored sorting algorithm.

If sorting inside a non-singleton bucket is required, use a mature pinned stdlib sort and prove the order; do not
write a generic sort.

===============================================================================
17. ERASED REPORTS FOR CROSS-SNAPSHOT EQUALITY
===============================================================================

`Diagnostic p` values from two different snapshots have different dependent ref types.  Compare deterministic
reports through an explicit erasure, not unsafe transport.

Define:

  ErasedAnchor :=
    EANode NodeKey
    EAFile FilePath
    EAPackage string
    EAProgram

  ErasedDiagnostic :=
    code
    primary ErasedAnchor
    related list ErasedAnchor
    stable structured payload projection

  erase_diagnostic :
    DiagnosticReason p -> ErasedDiagnostic

Do not put source syntax in the erased payload.

Use stable payload fields such as:
- target GoType;
- default target GoType;
- reason class.

The exact source expression remains reachable through the original typed anchor when working inside one p.

Prove:

- erase preserves code;
- erase preserves NodeKey/FilePath/PackageKey identity;
- erase preserves related-anchor order;
- `FilesEqual` programs produce equal canonically ordered erased diagnostic lists under the selected theorem;
- construction-list permutation produces the same erased diagnostics;
- backing AVL shape is unobservable.

Require equal ModuleSpec for a full analysis-result equality theorem if needed.  A narrower diagnostics theorem may
state that current diagnostic reports depend only on the file map.

===============================================================================
18. ONE COMPILER ENTRYPOINT
===============================================================================

Replace the current independent `go_compile` decision.

Current forbidden shape:

  go_compile p
      runs prog_ok p
      separately from analyze

Required:

  go_compile p
      calls/projections from analyze p
      uses the SAME retained IndexedProgram and AnalysisResult

Preferred public compilation artifact:

  Record CompilableProgram : Type := mkCompilable {
    cp_program : GoProgram;
    cp_indexed : IndexedProgram cp_program;
    cp_facts   : CompilationFacts cp_program cp_indexed
  }.

Hide the raw constructor behind a module signature if practical, so the canonical public production path is
AnalysisOK -> CompilableProgram.

Expose:

  cp_program
  cp_indexed
  cp_index
  cp_facts
  cp_ok : GoCompile cp_program

`cp_ok` is derived from `cf_valid`, not stored as an unrelated second proof.

Define a structured failure bundle:

  CompileFailure p ip :=
    diagnostics + nonempty proof

and make:

  go_compile

a projection of the one `ProgramAnalysis`.

Exact dependent signature may vary, but it must preserve:
- the same p;
- the same retained ip;
- structured diagnostics on failure;
- no second decision.

The old coarse:

  ErrTyping
  ErrPackageMainCount

must not remain the only production error evidence.

A legacy classification projection may remain only for regression compatibility:

  legacy_compile_class : erased diagnostics -> LegacyCompileClass

It must be a projection of analysis diagnostics, never a separate check.

===============================================================================
19. REMOVE PEER EXECUTABLE AUTHORITIES
===============================================================================

At FINAL, audit all call sites.

Required production facts:

- `analyze` is the one analysis execution root.
- `go_compile` projects it.
- SafeProgram is constructed only from CompilableProgram produced from AnalysisOK.
- rendering/emission only receives SafeProgram.

C2's `indexed_program_typedb` must be deleted/subsumed.

For current helpers:

- declarative `ProgramTyped` stays;
- semantic leaf booleans/reflection stay;
- `program_typedb` may remain only as an internal proof/reference checker if deleting it would create pointless
  churn;
- `prog_ok` may remain only as an internal specification reflection if needed for proof comparison.

If `program_typedb` or `prog_ok` remains:

- no production entrypoint calls it;
- docs label it proof/spec-only;
- it is not exported as a competing compiler;
- prove its result equals the analysis success class;
- Codex must review all call sites.

Prefer deletion or confinement when practical.

Do not keep:

  analyze
  go_compile-via-prog_ok
  indexed_program_typedb

as three peer whole-program executables.

===============================================================================
20. DECLARATIVE EXACTNESS THEOREMS
===============================================================================

The declarative whole-program judgment remains:

  GoCompile p := ProgValid p

or an exactly equivalent existing judgment.

Prove/gate:

A. Analysis success soundness

  AnalysisOK facts
    ->
  GoCompile p

B. Analysis success completeness

  GoCompile p
    ->
  exists facts,
    analyze_indexed p ip = AnalysisOK facts

for the canonical ip, or equivalent success predicate.

C. Failure soundness

  AnalysisFailed ds Hne
    ->
  ~ GoCompile p

D. Failure completeness

  ~ GoCompile p
    ->
  exists ds Hne,
    analyze_indexed p ip = AnalysisFailed ds Hne

E. Success/failure exclusivity

- success exposes facts and no diagnostics;
- failure exposes nonempty diagnostics and no facts.

F. `go_compile` exactness

- success iff analyze success iff GoCompile;
- failure carries the exact analyze diagnostics;
- CompilableProgram retains p/ip/facts.

G. Fact exactness

- every expression fact equals the source semantic result;
- every expression occurrence has exactly one fact on success;
- every resolved use fact equals `resolve_expr_const`;
- package facts exactly represent one main per represented package.

H. Diagnostic exactness

- every emitted diagnostic is sound for its code;
- every current invalidity class yields at least one diagnostic;
- all diagnostics are anchored in p;
- failure list is nonempty.

I. Existing external adequacy claim remains honest

- kernel theorem is internal exactness against GoCompile;
- equivalence to `go build ./...` remains differential evidence, not a kernel theorem.

===============================================================================
21. DOWNSTREAM SAFE/RENDER/EMIT INTEGRATION
===============================================================================

Update GoSafe:

- `GoSafe` remains evidence over CompilableProgram;
- SafeProgram retains the compiled artifact, hence p/ip/facts;
- `sp_program` still projects the original GoProgram;
- optional convenience:
    sp_indexed
    sp_facts
  may project retained evidence.

Do not make safety facts a second analysis.

Update GoRender / GoEmit only as necessary for changed projections:

- render original `sp_program`;
- do not render from ExprFact;
- do not render from ResolvedConst;
- do not choose spelling from semantic type;
- DirectoryImage provenance remains over one SafeProgram;
- generated bytes remain identical.

Rejected analysis:

- yields no CompilableProgram;
- yields no SafeProgram;
- yields no DirectoryImage through the canonical path.

Prove this chain.

===============================================================================
22. CURRENT REQUIRED DIAGNOSTIC FIXTURES
===============================================================================

Add universal theorems plus concrete scars.

22.1 Default integer overflow

A bare integer greater than platform int max:
- AnalysisFailed;
- one DCDefaultNotRepresentable at the literal ExprRef;
- payload default target = TInteger IInt.

22.2 Default float overflow

A bare exact float outside finite float64:
- primary literal ExprRef;
- DCDefaultNotRepresentable;
- default target TFloat F64.

22.3 Default complex overflow

A bare complex whose component cannot default to complex128:
- primary complex literal ExprRef;
- DCDefaultNotRepresentable;
- default target TComplex C128.

22.4 Invalid explicit integer conversion

  int8(128)

- DCInvalidConversion at int8 conversion ExprRef;
- target TInteger IInt8.

22.5 Fractional float to integer

- invalid integer conversion anchored at the conversion.

22.6 Nonzero-imaginary complex to scalar

- invalid scalar conversion anchored at the conversion.

22.7 Wrong-kind conversion

Example current AST:
  int(true)
or equivalent represented wrong-kind conversion.

- anchored at conversion;
- no generic unlocated ErrTyping.

22.8 Nested invalid conversion

Example:
  float64(int8(128))

- one diagnostic;
- primary int8 conversion;
- outer float64 conversion in related context;
- no duplicate outer diagnostic.

22.9 Duplicate mains in one file

- first DMain is related;
- later DMain is primary.

22.10 Duplicate mains across files

- canonical path/local order determines earlier/later;
- insertion order does not.

22.11 Three mains

- exactly two duplicate diagnostics;
- each later primary relates to the first canonical main.

22.12 Package with no main

- package has represented file(s);
- DCMissingMain at PackageRef;
- no fake file/node primary.

22.13 Empty program

- success;
- no package diagnostics;
- empty package facts map;
- no main required.

22.14 Multiple simultaneous failures

At least:
- two invalid expressions in different files;
- one duplicate-main package;
- one missing-main package.

Prove/report canonical order.

22.15 Reordered construction

Build the same semantic file map from permuted GoFileNode input:
- equal erased diagnostics;
- equal success/failure class;
- equal erased successful fact enumeration where applicable;
- generated bytes unchanged on success.

22.16 Repeated equal expressions

  println(1, 1)

On success:
- distinct ExprRefs;
- distinct NodeKey fact entries;
- equal semantic fact values where appropriate;
- no dedup by syntax value.

===============================================================================
23. FACT QUERY FIXTURES
===============================================================================

On a successful nested conversion expression:

- inner literal ExprFact:
    CIUntyped exact constant
    ef_use_resolved = None

- inner conversion ExprFact:
    CITyped at its target
    ef_use_resolved = None if it is an operand

- outer println argument ExprFact:
    current ConstInfo
    ef_use_resolved = Some exact ResolvedConst

Prove:

- `resolved_type_at` matches current GoTypes resolution;
- `resolved_constant_at` matches current exact/runtime coherence;
- no rerounding occurs;
- facts do not alter GoSafe evaluation;
- querying by a different ExprRef returns that occurrence's own fact.

===============================================================================
24. DETERMINISTIC REPORT FORMATTING
===============================================================================

C3 may add a pure human-readable projection, but keep it outside the proof-core diagnostic reason.

Without SourceMap, report anchors as:

- node:
    path + local occurrence id
- file:
    path
- package:
    package key
- program:
    program

Include:
- stable diagnostic code;
- structured target/default information;
- related anchor keys.

Do not claim source line/column yet.

Formatting is not an authority:
- proofs and compiler decisions use structured diagnostics;
- English text is derived.

No raw URL/path outside intrinsic FilePath/PackageKey identities.

===============================================================================
25. ROOT REVIEW BARRIER
===============================================================================

Stop intentionally after ALL of the following are true:

- C2 status marked human-approved;
- IndexedProgram p exists in GoIndex and is axiom-free;
- one standard NodeKey FMapAVL wrapper exists;
- NodeKey canonical elements theorem exists;
- GoTypes has a shared `const_info_step` or exact equivalent;
- no semantic conversion logic is duplicated;
- PackageRef exists and is validated;
- structured diagnostic reason/anchor types exist;
- ExprFact and standard-map-backed ExprFactTable exist;
- package main-ref collection exists;
- analyze_indexed exists;
- public analyze builds/retains one index;
- current expression failures localize innermost causes;
- package duplicate/no-main diagnostics are anchored;
- AnalysisOK iff GoCompile theorem core is green;
- AnalysisFailed is nonempty and implies rejection;
- CompilableProgram retains p/ip/facts;
- go_compile projects analyze rather than prog_ok;
- no typed/resolved/copied AST;
- no per-node source recovery;
- no per-node recursive const_info rescan;
- no custom collection;
- generated bytes remain unchanged.

Commit:

  milestone(root): C3 — one retained indexed analysis root with anchored facts and diagnostics

Codex repairs:

  review(root): C3 —

ROOT review must specifically attack:

- index reconstruction inside facts/queries;
- GoTypes still owning the production indexed traversal;
- `go_compile` still calling prog_ok independently;
- const_info recursively recomputed for every expression fact;
- copied SourceOccurrence/GoExpr stored in facts;
- raw NodeKey facts without validity/kind proof;
- unvalidated raw package strings as anchors;
- fail-soft fallback diagnostics;
- duplicate main evidence overwritten by map add;
- empty AnalysisFailed;
- wrong nested-conversion primary;
- one diagnostic emitted at every ancestor;
- CompilableProgram constructible from a parallel path;
- custom map/sort implementation;
- semantic import into GoIndex;
- generated-byte drift;
- accidental C4 work.

Do not continue to FINAL until ROOT is non-stale GREEN.

===============================================================================
26. FINAL INTEGRATION BARRIER
===============================================================================

After ROOT GREEN, continue directly.

Complete:

- every current diagnostic code/payload;
- canonical diagnostics flattening;
- erased cross-snapshot reports;
- FilesEqual/permutation diagnostic order theorem;
- successful fact enumeration determinism;
- old peer checker removal/confinement;
- GoTypes -> GoIndex temporary coupling removal where possible;
- GoSafe/GoRender/GoEmit projection updates;
- full e2e fixtures;
- report formatting;
- docs;
- collection audit;
- gate;
- whole-theory audit;
- generated-byte comparison;
- staged-tree verification.

Commit:

  milestone(final): C3 — complete deterministic occurrence-indexed compilation facts and diagnostics

Codex repairs:

  review(final): C3 —

FINAL review must specifically attack:

- stale production call to prog_ok/program_typedb/indexed_program_typedb;
- index rebuilt by go_compile after analyze;
- different index retained in CompilableProgram;
- fact/diagnostic order depending on input list or AVL shape;
- diagnostic equality asserted directly across dependent snapshots instead of through erasure;
- multiple diagnostics silently overwritten at one NodeKey;
- package member bucket quadratic append;
- package diagnostics missing packages with zero main;
- duplicate main order based on incidental traversal/insertion;
- facts exposed on failure;
- diagnostics exposed on success;
- renderer using semantic facts rather than source;
- source line claims without SourceMap;
- stale docs describing coarse errors;
- un-gated public theorem surfaces;
- assumption drift;
- generated-byte drift;
- accidental C4 work.

After FINAL GREEN:

1. rerun full `make check`;
2. rerun staged-tree verification;
3. verify recursive `.go` + go.mod bytes against pristine generated artifact;
4. update SOURCE_FOREST_STATUS with activation/root/final/repair SHAs, review timestamps/results, gate count,
   verification, byte comparison, push, and human disposition pending;
5. fast-forward push;
6. notify Rob;
7. stop the loop;
8. do not begin C4.

===============================================================================
27. REQUIRED GATE SURFACES
===============================================================================

At minimum add readable Print Assumptions coverage for:

IndexedProgram:
- canonical construction;
- retained index projection;
- no-rebuild/query bridge.

NodeKey map:
- ordered equality;
- find/add laws;
- canonical elements under Equal.

PackageRef:
- construction soundness;
- equality/key injectivity;
- represented-package witness.

Diagnostics:
- primary/related anchor validity;
- code-specific soundness;
- nested primary/related theorem;
- duplicate-main same-package/order theorem;
- missing-main exactness;
- erasure correctness;
- deterministic erased ordering.

Facts:
- ExprFactTable key validity;
- expression-kind exactness;
- expression fact soundness;
- expression fact completeness;
- no non-expression entries;
- resolved-use exactness;
- package fact soundness/completeness;
- total typed-ref query.

Analysis:
- success soundness/completeness;
- failure soundness/completeness;
- nonempty failure;
- success/failure exclusivity;
- no facts on failure;
- no diagnostics on success.

Compiler:
- go_compile projection theorem;
- go_compile sound/complete;
- CompilableProgram retains exact p/ip/facts;
- rejected analysis cannot produce CompilableProgram/SafeProgram/DirectoryImage.

Order:
- diagnostics canonical;
- FilesEqual erased diagnostics equality;
- construction permutation equality;
- fact enumeration canonical.

Preserve all accepted C0-C2 surfaces.

===============================================================================
28. PERFORMANCE / IMPLEMENTATION INSPECTION
===============================================================================

The final code shape must visibly establish:

- one `Snap.index_program p` per `analyze p`;
- no second index in go_compile;
- one canonical file-map enumeration;
- one `Snap.visit_file` per analyzed file;
- bounded linear folds over each visit stream;
- bottom-up expression analysis;
- one semantic step per expression occurrence;
- standard NodeKey map lookup/update;
- standard PackageMap lookup/update;
- no recursive whole-subtree `const_info` call per occurrence;
- no `source_occurrence_of_ref` / `node_at` hot-path recovery;
- no repeated all-files package scan;
- no repeated append in large package buckets;
- no custom sort;
- no structural syntax equality lookup/dedup;
- no copied AST facts.

Run transient pinned-container probes for representative:

- conversion chain depth;
- sibling expression count;
- file count;
- mains per package;
- diagnostics count.

Use at least:
  1
  10
  100
  practical 1,000 where comfortable.

Record raw observations honestly.

Do not claim a kernel machine-complexity theorem unless proved.

===============================================================================
29. DOCUMENTATION AND LIVING AUDITS
===============================================================================

Update:

- CLAUDE.md
- ARCHITECTURE.md
- PROGRESS.md
- README.md as appropriate
- dune synopsis
- .review/SOURCE_FOREST_MASTER_PLAN.md
- .review/SOURCE_FOREST_STATUS.md
- .review/COLLECTION_AUDIT.md
- gate comments

The permanent architecture must say:

- indexing is a mandatory compiler first phase;
- IndexedProgram is retained;
- GoCompile is where GoIndex and GoTypes meet;
- analyze is the one production analysis root;
- go_compile is a projection;
- CompilationFacts are occurrence-keyed semantic decorations;
- facts contain no syntax copies;
- diagnostics are exact-snapshot anchored;
- reports are deterministic through canonical NodeKey/FilePath/PackageKey ordering;
- rendering remains source-driven.

Update COLLECTION_AUDIT with:

- NodeKeyMapBase:
    FMapAVL over NodeKey;
- ExprFactTable:
    standard NodeKey map;
- node diagnostic buckets:
    standard NodeKey map -> ordered list value;
- package main buckets:
    standard PackageMap -> ordered DeclRef list;
- package facts:
    standard PackageMap;
- final diagnostic list:
    ordered derived report enumeration;
- temporary analysis maps:
    standard maps, not persistent source authorities.

Correct the audit whenever any current collection role changes.

Do not add a brittle regex gate pretending to prove architecture.

If a new painful lesson emerges during implementation/review, add only the durable, non-repetitive lesson.  Do not
add a lesson merely because C3 exists.

===============================================================================
30. FULL VERIFICATION
===============================================================================

Run:

  make check

Required green:

- all Rocq modules;
- readable gate exact count;
- whole-theory assumption audit;
- certified-module coverage;
- adversarial self-tests A-E;
- Go Emit;
- sink tests;
- pinned Go `go build ./...` differential fixtures;
- new structured diagnostic fixtures;
- new fact queries;
- order permutation fixtures;
- generated go.mod byte identity;
- recursive generated `.go` byte identity;
- no untracked generated/control/temp residue.

Then run the staged-tree pre-commit verification from the exact staged export.

No claim based only on a working-tree build.

===============================================================================
31. FORBIDDEN DURING C3
===============================================================================

Do not add:

- source identifiers;
- type-name syntax;
- name resolution;
- imports;
- uintptr;
- rune constants;
- pointers;
- named types;
- aliases;
- arrays;
- slices;
- maps;
- channels;
- function types;
- structs;
- interfaces;
- methods;
- arithmetic;
- general calls;
- selectors;
- assignments;
- source spans;
- file/line mapping;
- decimal-printer migration;
- Qc/FloatConst migration;
- any new Go behavior.

Do not:

- modify GoIndex to know semantic types;
- leave GoTypes as the production owner of indexed compilation;
- create GoAnalysis as a second public compiler authority;
- retain multiple production analysis roots;
- create a typed/resolved/located AST forest;
- store SourceOccurrence/GoExpr in CompilationFacts;
- pass raw local positive ids as ordinary compiler identity;
- use raw path + id where a typed ref is available;
- use raw string package anchors;
- rebuild index per query;
- recover syntax from refs on the hot path;
- recursively analyze each subtree once per ancestor;
- invent collection storage;
- sort with a project-authored generic algorithm;
- silently overwrite diagnostic/fact duplicates;
- default analysis failure to success/empty;
- render from semantic facts;
- claim source line/column.

If a required design cannot be implemented without violating a standing law, record an ARCHITECTURAL CONFLICT,
notify Rob, and stop.  Do not silently weaken the contract.

===============================================================================
32. ACCEPTANCE
===============================================================================

C3 is complete only when:

- C2 is recorded human-approved;
- IndexedProgram p exists;
- one retained index is built per analysis;
- GoCompile is the sole index+semantics integration layer;
- analyze is the one production root;
- go_compile is a projection;
- CompilableProgram retains exact p/ip/facts;
- current successful expression facts are complete and exact;
- current package facts are complete and exact;
- every current failure has a valid structured anchor and reason;
- nested failure primary is innermost;
- duplicate-main primary/related order is canonical;
- missing-main uses PackageRef;
- failure diagnostics are nonempty;
- success has facts and no diagnostics;
- failure has diagnostics and no facts;
- deterministic erased reports are proved under FilesEqual/permutation;
- no copied/typed/resolved AST;
- no raw package/path/node identity duplication;
- no custom collection;
- no hot-path source recovery;
- no repeated subtree analysis;
- no peer production checker;
- GoSafe/GoRender/GoEmit remain source-faithful;
- zero assumptions;
- full check green;
- staged check green;
- generated bytes unchanged;
- ROOT Codex GREEN;
- FINAL Codex GREEN;
- fast-forward pushed;
- loop stopped;
- C4 not begun.

===============================================================================
33. FINAL ARCHITECTURE SNAPSHOT
===============================================================================

Source:

  GoProgram p
    ModuleSpec
    FileMap FilePath GoSourceFile

Structural elaboration:

  IndexedProgram p
    one Snap.SyntaxIndex p

Occurrence identity:

  FileRef p
  NodeRef p
  ExprRef p
  DeclRef p
  NodeKey = FilePath * positive

Semantic foundation:

  GoTypes
    const_info_step
    const_info
    convert_const
    resolve_const_info
    ResolveExpr / ProgramTyped

Analysis:

  analyze p
    let ip := index_program p
    analyze_indexed p ip

  AnalysisResult p ip
    AnalysisOK CompilationFacts
    AnalysisFailed nonempty diagnostics

Facts:

  NodeKeyMap ExprFact
    ConstInfo
    optional ResolvedConst

  PackageMap PackageFact
    exact main DeclRef

Diagnostics:

  exact-snapshot anchors
    AtNode
    AtFile
    AtPackage
    AtProgram

  current codes
    invalid conversion
    default not representable
    duplicate main
    missing main

Compiler capability:

  CompilableProgram
    original p
    retained ip
    facts

Safety/rendering:

  SafeProgram
    compiled artifact
    safety evidence

  GoRender / GoEmit
    original source only

The source tree remains dumb and faithful.
The index owns structure.
GoTypes owns semantic relations.
GoCompile owns analysis, facts, diagnostics, and acceptance.
One program. One index. One analysis.

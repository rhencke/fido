# Archived FCB project-library set v2

These files are the exact byte-for-byte FCB v2 set that existed in the model project libraries before
`FCB-A002-GIT-CANONICAL-FCB-STORAGE` moved the live corpus into Git.

The set's manifest file SHA-256 is `6a569c8c8e0963344c79a4cd0800e9c8495eecde54bb9a6d78f732731b3e8682`. The manifest verifies the fourteen FCB files and
intentionally excludes itself. This archive is provenance only. The live corpus is `../../current/`.

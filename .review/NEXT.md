# Active task: the review-to-work authorization-sequence governance repair

Review: implementation

One retained governance repair: make the review-to-work release sequence truthful and authorization-safe,
and close the bounded governance archaeology due at this edit. This is a governance slice — not a technical,
semantic, artifact, or performance repair. It authorizes no later work and accepts no candidate.

## Authorized objective

`ARCHITECTURE.md` is the durable owner of the review-to-work release rule; `CLAUDE.md` is its faithful
operator mirror (not a peer authority). Both must state the same exact ordered transition:

1. every required criterion report over one immutable candidate is completed and frozen;
2. only after that join, one fresh role produces a private synthesis and causal recommendation for Rob;
3. the private synthesis creates no work contract and authorizes no work;
4. Rob explicitly authorizes one exact lawful causal-root slice, after separately selecting it;
5. only then does a fresh context create one self-contained outbound work contract with exactly that
   authorized substantive work, stating its own requirements and needing no private review material;
6. if Rob authorizes no slice, the process stops at the private recommendation and creates or releases no
   contract, draft, implementation prompt, or task — the no-authorization state is terminal;
7. a retained implementation returns as a new immutable candidate for a fresh whole-candidate review before
   another review-derived task; and
8. candidate acceptance stays a later, separate Rob decision — work authorization is not acceptance.

The synthesis-produces-the-contract claim and the ambiguous "no work is manufactured" closing are removed
from both documents; no compatibility path survives beside the new order.

## Bounded concurrent cleanup (closure point is this edit)

- `.dockerignore`: replace the extinct `make perf`/`.review/PERFORMANCE.tsv` comment and ignore entry with
  `/.review/perf/runs/` and a live comment — append-only raw benchmark sessions are tracked review
  evidence, never build inputs.
- `.editorconfig`: replace the nonexistent `PROBE_ENVIRONMENT.tsv` example with a format-generic
  trailing-empty-field explanation; keep the `*.{tsv,csv}` rules.
- `DECISIONS.md`: delete only the dated `Supersedes the prior PACKAGE-MAIN-CARRIER rule … 2026-08-19`
  sentence; keep the current decision and reopen trigger.

## Preservation and non-goals

Preserve every byte outside the six authorized governance/context files and the mechanically required
candidate-bound performance-evidence refresh: all source, proofs, semantics, the three-way
`Compiled`/`Rejected`/`OutsideScope` distinction and unsupported boundaries, axiom-free closure and the
assumption audits, the one supported BuildKit DAG (one project solve, one Dune theory build, proof/emit
overlap, both-marker join, pinned-Go validation, exact generated-program path/byte/mode comparison,
fail-closed errors), cache-independent correctness and the one 120-second fail-closed budget owner, the
no-host Rocq/Python boundary, one semantic authority, one publication route. No binding/result/issue/
disposition redesign, no theorem or constructor change, no measurement/parser/selector/accounting/
validator/reachability repair, no Make/hook/schema addition, no performance optimization or new success
threshold, no roadmap rewrite, no accepted-decision reopening, no C4 completion claim, no candidate
acceptance, and no selection or authorization of the next technical task. `ARCHITECTURE.md`, `DECISIONS.md`,
`ROADMAP.md`, and the structured status/opportunity ledgers already own the one-DAG, semantic/result,
performance-lifecycle, and frontier facts; their narration is not relocated here.

## Frontier

C5 stays historically accepted; C4 stays implemented but unaccepted and incomplete; C6 and all dependent
work stay frozen. `make check` on the pinned toolchain is the supported run.

## Candidate-bound performance evidence

Two-commit order, append-only. First commit the six-file governance implementation; then benchmark that
exact clean commit and, in a later evidence-only commit, add exactly one immutable timestamped session
file `.review/perf/runs/YYYYMMDDTHHMMSSZ-<12-hex-impl-commit>.tsv`. That file names the measured commit,
tree, and performance-input digest, records three cold-to-immediate-warm and three staged pairs, and
re-attests the unchanged opportunity ledger by path and SHA-256 (no status change). Every pre-existing
performance basis, row, graph, derived view, and opportunity stays byte-identical — nothing is rebased,
relabeled, or regenerated; the prior comparison medians remain honestly historical in the existing
ledgers. `tools/performance-input-digest.sh` already excludes `.review/perf/*`, so the implementation and
evidence commits share one performance-input digest. The only hard bound is the existing 120-second
fail-closed budget; this slice carries no wall target and adds none. Every open opportunity stays open;
unknown savings stay unknown.

## Stop conditions

Return `BLOCKED_FOR_ROB` on: the starting archive/commit/index not matching; the ordered rule needing a new
authority or process surface to state consistently; any change to review criteria, the weakest-result rule,
gate attestation, acceptance authority, or another governance decision; a still-binding invariant in the
pruned history with no evident durable owner; an authored edit leaving the six-file surface; validation or
evidence refresh needing a source/proof/gate/schema/selector/accounting/artifact/build-graph change; any
semantic or generated-program byte changing (the named performance-evidence views are the sole
generated-byte exception, through their existing generators); a confirmed hard-budget failure or unexplained
regression; a needed weakened/skipped check; or a second causal root proving inseparable.

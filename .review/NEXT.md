# Active task: the C4 Index physical-closure and budget-authority candidate

Review: none

The current frozen candidate **closes the Index physical split**: root `Index.v` **is** the canonical Core
owner (ProgramIndex, FileRef/NodeRef, node projections, generic parent/child, enumeration, and FileRef
construction) with `Index/Core.v` deleted; the raw numbering laws live in `Index.BuildLaws` and `Index.Build`
is executable-only; the one-way graph is `Model → Build → BuildLaws → Index → { Child, Refs } → Edges`. No
production consumer names `Index.Build`/`Index.BuildLaws`; consumers import the exact owners they use. The
120-second warmed `make check` budget is owned solely by `tools/check-budget.sh` (unoverrideable, no Make
variable), `make check` is the sole full-verification entry, and the staged pre-commit routes through the same
runner. A permanent owner-control gate enforces the physical symbol ownership. It is move-only — no semantic
definition, theorem strength, proof assumption, generated byte, execution path, or trusted boundary changed;
`make check` is green and the generated Go bytes are byte-identical. The C4 evidence-DAG semantics it
reorganizes are described below and are unchanged.

The C4 static-authority subsystem is rebuilt as an exact-evidence DAG rooted at one `Compilable.Program p`:

- **Index** is the direct shallow occurrence authority — a finite file map of finite position maps of shallow
  cells, one source-ordered traversal; RNode trees, flat scans, and list-scan reads are gone.
- **PackageIdentity** retains the package surface, position-selector `PackageRef`s, exact module-path and
  complete-import-path components, and the default executable name over the complete import path (with terminal
  semantic-major stripping).
- **Bindings** is the structural binding foundation: establishments, scopes, ordinary resolution, declaration
  groups keyed by exact scope and spelling with unique/redeclared status, const-inheritance and short-declaration
  facts, and the fixed `func main()` as a real package-scope function declaration that joins the one declaration
  group keyed by scope and spelling (a redeclared `main` group is `OrdinaryRedeclared`) and resolves through
  ordinary scope (recursive/cross-file, with local `main` shadowing), `MainMissing`/`MainOne`/`MainMultiple` a
  distinguished projection over those declarations for executable-entry multiplicity.
- **Analysis** is the sole fact and issue authority: one applicability-first fact per applicable family and never
  a fabricated success — a family that would apply but is blocked by an exact prerequisite yields a non-success
  `Dependency`-carrying result (VDependent/ADependent/SDependent/TDependent) that adds no diagnostic and no
  duplicate; the canonical issue sequence with coexistence (no diagnostic-suppresses-boundary rule), ordinary
  redeclaration, the selected-package fresh-output preflight, and the complete disposition algebra. Every issue
  has an exact ordinal identity — an `IssueRef` into the one `result_issues` sequence, not a payload at a position.
- **Report** projects those exact issue identities and nothing else, with proved bidirectional-membership, exact
  identity, class-partition, no-collapse, payload, and stable-order laws.
- **Compilable** is the sealed `C4_PUBLIC` surface: `compile` is the sole source of the abstract branch objects
  `Program`/`Rejection`/`Outside`, selected by the transparent `disposition` over the one retained `Analysis.Result`;
  each compilation retains that exact result with a provenance proof it is the canonical `analyze p`, so a rebuilt
  peer result cannot back a capability and a rejected program admits no `Program`; the record makers and the private
  composer are absent from the client namespace.
- **Emit** is one generic evidence-indexed boundary: `Emit.Image cp Evidence evidence`, `of_compiled`, and
  `of_evidence`, all over an exact compiled `Program` with no source/bytes route; `transport` is Render over the
  exact indexed source. The vacuous `Safe` module is deleted; real safety evidence is a future opt-in C15 layer.

`make check` is green (prove + e2e + byte-compare + diet + host-python); the generated bytes are byte-identical.
The formal-vs-Go acceptance differential begins from one named `Syntax.Program` per case: the formal side executes
`compile` (a proven disposition), the external side is pure Render of that same source run through the pinned Go
toolchain, and the two verdicts are compared (a rejected program's rendered source must fail `go build`; a compiled
program's must build). The gate corpus (layer policy, one-build, sealed-abstract-branch and `neg_*` unforgeability
controls, positive control, forge-provenance controls) and the governing corpus are updated with the code.

## Work in progress: C4 Exact Binding Provenance Through Analysis

Two checkpoints layer on the frozen candidate above. **Checkpoint 0 landed** (commit `852b6b5`, pushed, green
within the 120-second budget) as one byte-neutral commit: `make check`'s hard budget now times the *complete*
supported path — builder/pinned-image setup, every former Make prerequisite, `prove`, `e2e`, and the
generated-artifact comparison — instead of starting the clock after the prerequisites; the staged pre-commit hook
times its complete staged verification the same way; budget adversaries cover the pre-core, staged, malformed,
producer-failure, command-line-raise, and unbudgeted-target cases; and the unused `Collections`/`FilePath` imports
(with their layer-policy edges) were deleted from `Index/Edges.v`. No semantic definition, theorem, generated byte,
or trusted boundary changed.

**Checkpoint 1 is in progress** — the exact-provenance retention phase has landed and the vm-cliff it once risked is
resolved. §14 stands: Analysis's whole fact algebra is indexed over the exact Binding phase `bp` (`Cause bp`,
`Requirement bp`, `Diagnostic bp`, `IssueCause bp`, …). The cliff — `bp` in the *type* of a concrete-program
`vm_compute` goal forcing the whole `BindingPhase` to normalize (~480 s) — is avoided by the resolution the contract
prescribes (§15.2/§27): concrete controls observe **bp-free descriptive views** (`CauseView`/`ReqView` in Report,
§20), projected one-way from the exact objects so no goal type carries `bp`; the exact `bp`-indexed refs are checked
only in abstract-`bp` kernel laws where the kernel never normalizes `bp`.

Landed and green (`make check` warmed ~49–90 s / budget 120 s, generated Go byte-identical):

- every resolution-derived cause/requirement/dependency retains the exact `ResolutionRef` (unbound, predeclared,
  bound-object, source-bound, and redeclared-root cases);
- `ShortDuplicate` retains its exact `ShortDuplicateDecision` and canonical certificate; const causes retain the
  exact `ConstSpecJudgmentRef`;
- Binding exposes the exact `redeclaration_roots` enumeration (one exact `RedeclRoot` per redeclared group).

Remaining: the exact redeclaration diagnostic topology (`DRedeclaredGroup`/`RootGroup`/`RedeclaredGroupCause` over
exact `RedeclRoot` + `RedeclaredUseRef` contexts, matched by exact `RedeclRoot` identity), moving the descriptive
group/related-node views to Report, deleting the descriptive `group_view`/`redecl_view`/`redeclared_groups`/
`same_groupref` route, the full law/fixture/hostile-control suite, and the freeze.

## Status

The frozen candidate above remains `IMPLEMENTED_NOT_ACCEPTED` (C5 stays historically accepted; C6 roots beyond the
cutover stay frozen until acceptance; `make check` on the pinned toolchain is the supported run; only Rob accepts).
The C4 Binding Provenance contract's exact-provenance retention phase is committed and green; the redeclaration
diagnostic topology and its controls remain before the candidate is frozen for review.

## Stop conditions

Stop and ask Rob on a falsified accepted lower fact (which triggers targeted causal dependency retreat), a
representable case that cannot be classified `Compiled`/`Rejected`/`OutsideScope` without narrowing syntax, exact
Binding indexing/retention that cannot be brought off the vm-compute cliff without weakening §14, or any scope
or acceptance decision.

# Active task: freeze the C4 authority-cutover candidate and submit for review

Review: none

The atomic C4 authority cutover is **complete and green**. It replaces the whole C4 static-authority topology
with one sealed public surface and one analysis authority:

- **Index** is the direct shallow occurrence authority — a finite file map of finite position maps of shallow
  cells; RNode trees, flat scans, and list-scan reads are gone.
- **PackageIdentity** retains the package surface and position-selector `PackageRef`s over the new Index.
- **Bindings** qualifies the fixed-spelling `func main()` occurrence through PackageIdentity as `MainDeclRef s pr`
  — a real package function establishment — with `MainMissing`/`MainOne`/`MainMultiple` multiplicity, independent
  of the ordinary declaration group keyed by `main`.
- **Analysis** is the sole fact and issue authority: per-occurrence value/application/statement/type-use outcomes
  with no cross-node blocking (independent parent and child issues coexist), the declaration outcomes (const
  missing-init / result-count / short duplicate / later-meaning boundary), the selected-nonempty-package
  fresh-output preflight, the one canonical issue table, and the applicability + complete 5-way disposition
  algebra. `Compilable.Facts` and `Compilable.Packages` are deleted.
- **Report** is a pure projection of the canonical issue table — no cause, order, dedup, or fallback.
- **Compilable** is the sealed `C4_PUBLIC` surface: `compile` the sole `Decision` source, `inspect` its sole
  eliminator, `compiled_prog` the sole payload-gated `Prog` projection, `Prog.Program` opaque with a private
  admissibility-gated maker, and no public elaborator or decision constructor.
- **Safe/Emit** consume the exact compiled payload; `of_safe` is the sole image route.

`make check` is green (prove + e2e + byte-compare + diet + mutants); the generated bytes are byte-identical. The
pre-existing `multi_valid` blocker is resolved by the selection-based preflight (two selected packages → collision
non-applicable). The gate corpus (layer policy, one-build, `neg_*` unforgeability controls, positive control) and
the governing corpus are updated with the code.

## Current action

Freeze this candidate — a source archive whose ZIP comment is its exact commit and whose bytes reconstruct the
tree — and submit it for the sixteen-pass Wirth review (`ARCHITECTURE.md` §1 "Review and acceptance"). Rob supplies
the archive to the review and alone accepts.

## Status

`IMPLEMENTED_NOT_ACCEPTED`. C5 stays historically accepted; the C6 roots beyond this cutover stay frozen until the
candidate is accepted. `make check` on the pinned toolchain is the supported run; only Rob accepts.

## Stop conditions

Stop and ask Rob on a falsified accepted lower fact (which triggers targeted causal dependency retreat), a
representable case that cannot be classified `Compiled`/`Rejected`/`OutsideScope` without narrowing syntax, or any
scope or acceptance decision.

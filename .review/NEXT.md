# C6 — active task

Review: none

The active work is the **C4 exact-elaboration retreat repair**, now on its third review cycle. Synthesis
`b1512c48` ("off we go") reviewed `b0d8dc7`, returned `REVIEW_BLOCKED` (grade F), and deduplicated its findings
into **nine root causes RC-01..RC-09** — a compiler-interior rebuild. It overturned the earlier "O02 is a
`vm_compute` wall that needs a toolchain change" conclusion: the one-pass fold is the required in-model path,
and that has now been proved. Everything past the retreat stays frozen until Rob closes it. Rob alone closes
and accepts.

## Landed, green through the full gate and pushed

- **RC-01 core — one occurrence authority** (`e100f84`): a single structural fold (`occ_file`) is the sole
  occurrence authority; each node's member carries its exact source view. `meta_of` reads a cheap viewless Meta
  index derived from that same fold; `member_of` reads the retained members. The source re-walk
  (`source_occurrence_at`), the Meta-threading builders (`build_*`), the subtree-end peers (`end_*`), and the
  whole agreement forest that reconciled the two old authorities are deleted. Index.v: 2153 → 532 lines.
  Generated bytes identical; the `vm_compute` wall is gone in-model, no toolchain change, nothing added to the
  TCB. (This sits on top of the second cycle's O01/O03/O04/O05, already landed.)

## Blocked — a conflict for Rob (rules 2 and 10)

RC-01's "Required outcome" also asks (item 5 / synthesis line 362) to **seal the occurrence/index/ref
constructors off the client surface** (delete public `MakeOccurrence`/`MakeIndex`/`MakeFileRef`/`MakeNodeRef`).
This **conflicts with the witness's `vm_compute` proof of `Admissible demo`**, and the conflict is confirmed
empirically:

- Opaque sealing (a `Module Type` ascription that hides a record's constructor) makes the occurrence type and
  its projections non-reducible. The occurrence layer is *in* the `vm_compute` path (`all_diags` → `occ_diag` →
  `view_expr`/`occurrence_kind` over `occurrences_file`). A scratch that sealed just `occurrences_file` made
  `demo_valid`'s `vm_compute` **OOM — `Killed` at ~788s**, the same accumulator cliff as before. Sealed
  occurrences behave to the VM like the opaque map did.
- Compilable/Safe/Emit seal cleanly because none of them is `vm_compute`-reduced in a proof; the occurrence
  layer is different — it is computed.

The full write-up for the reviewer — mechanism, the empirical scratch, and the options — is
`.review/RC01_SEALING_CONFLICT.md`. This is a foundational fork, so it is Rob's call and the reviewer's to
adjudicate, not an autonomous route-around:

- **A (cheap):** do not opaque-seal the occurrence/index/ref layer; keep it constructor-transparent because the
  `vm_compute` witness needs it. RC-01.5's constructor-hiding stays unmet for these types (documented reason);
  the other seals stand.
- **B (large):** re-prove `Admissible` (and the negative controls) from the sealed API's exposed *theorems*
  instead of by `vm_compute`, so the occurrence layer can be opaque-sealed. This changes the demo-validation
  strategy from computational to lemma-based across the system.

## The rest of the nine roots

RC-01.4 (kind/role-refined references) is authorized by the synthesis and buildable transparently, but it is
part of the same occurrence/ref API whose sealing shape is under decision, and **RC-02 (intrinsic binding)
depends on it**. RC-03..RC-09 (type identity, report algebra, retained phase, Program = exact Compiled,
soundness/completeness, branch-complete evidence, governance) follow in sequence. All wait behind the fork
above.

## The stop

`IMPLEMENTED_NOT_ACCEPTED`. RC-01 core is landed and green; the remainder waits on Rob's decision about the
sealing-vs-`vm_compute` fork (which shapes the whole downstream approach). `make check` on the pinned toolchain
is the supported run.

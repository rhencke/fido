# Active task: freeze the R1–R7 candidate and submit for review

Review: none

The C4 exact-elaboration retreat repair — seven roots R1–R7, dispatched from the archive-authoritative
sixteen-pass synthesis `3ac0246c` — is **complete and green**:

- **R1** the intrinsic `Index` and its position-selector references (a malformed identity has no inhabitant);
- **R2** exact package/binding/scope identity with declaration-kind visibility;
- **R3** the `Facts → Report → Compilable` cut over one retained phase, built once;
- **R4** the semantic partition (typed-`complex` → OutsideScope; `iota`/`nil`/builtin-as-value → Rejected; no
  `None` conflating invalid/absent/unmodelled);
- **R5** the transparent capability/provenance chain (`Compiled` carries the exact `Program`; `Emit.of_safe`
  is the sole live image constructor; unforgeability is intrinsic, not opaque);
- **R6** the exact three-way theorem family (`accepted_iff_admissible`, `rejects_iff_diags`, `outsides_iff`,
  `decision_total` with its pairwise exclusions, `program_source`/`program_core`), the branch-payload and
  declaration-visibility fixtures, and the assumption/gate controls extended to the proof-bearing e2e fixtures;
- **R7** this corpus, finalized to current truth.

`make check` is green and the generated bytes are byte-identical throughout. R4's deeper multi-issue causal
chain (predecessor/root/blocked) is deferred as a no-op in the current single-issue domain.

## Current action

Freeze this candidate — a source archive whose ZIP comment is its exact commit and whose bytes reconstruct the
tree — and submit it for the sixteen-pass Wirth review (`ARCHITECTURE.md` §1 "Review and acceptance"). Rob
supplies the archive to the review and alone accepts.

## Status

`IMPLEMENTED_NOT_ACCEPTED`. C5 stays historically accepted; the C6 roots beyond this repair (named type
identity, defined/alias types, nonconstant conversions, the type environment, declarations, and all runtime)
stay frozen until the repaired candidate is accepted. `make check` on the pinned toolchain is the supported
run; only Rob accepts.

## Stop conditions

Stop and ask Rob on a falsified accepted lower fact (which triggers targeted causal dependency retreat), a
representable case that cannot be classified `Compiled`/`Rejected`/`OutsideScope` without narrowing syntax, or
any scope or acceptance decision.

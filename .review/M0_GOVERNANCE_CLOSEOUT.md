# M0 — Governance Closeout

> **Live authority.** Installed by accepted amendment `FCB-A008-STRICT-CHECKPOINT-SCOPE-AND-M0-CLOSEOUT` and
> governed by Governance `D-29`. Its identity is its Git blob at the exact ref resolved for the task.
> **C4 is ACCEPTED** under the human disposition `C4-ACCEPT-39ea7e3`. The exact accepted ref is owned by the
> A008 amendment register row in `.review/fcb/current/FIDO_FCB_GOVERNANCE.md` and appears nowhere else.
> **M0 is the sole active work.** M1 through M4 and C5 are forbidden until Rob accepts M0.

## 1. Purpose

Publish one terse, coherent current state after C4 acceptance. Retire C4 repair state to Git history. Prove
that the existing generated human-act view and typed reference relation represent the new M0 state.

M0 does not absorb M1, M2, M3 or M4 work. That is the whole point of it existing separately.

## 2. Allowed changes

M0 may change only:

- the live FCB authority, data and generated views under `.review/fcb/current`;
- the amendment record for A008, beside the other accepted amendments;
- `.review/NEXT_STEPS.md`;
- `.review/OPEN_QUESTIONS.md`;
- `.review/REVIEW_REQUEST.md`;
- `.review/M_SERIES_PLAN.md`;
- `.review/M0_GOVERNANCE_CLOSEOUT.md`;
- `.review/M0_OBLIGATION_MATRIX.tsv`;
- `.review/SOURCE_FOREST_STATUS.md`, only to replace stale current-checkpoint state;
- `PROGRESS.md`, only to remove stale checkpoint and authorization state and defer to the canonical owners.
  Its size, archaeology and structure belong to M1;
- root `CLAUDE.md`, only to install D-28's implementer rule;
- typed reference-manifest rows and generated human-review output required by those exact changes.

M0 may delete the no-longer-live C4 repair-21 directive, matrix and closure audit once every live reference to
them is removed. Git history is their archive.

M0 may **not** change any tracked Rocq source, any OCaml source, the container build, the `Makefile`, Dune
files, the hooks, any tool, the generated module, the end-to-end fixtures or goldens, public theorem or
capability topology, or proof and build behaviour.

If the exact current-state transition cannot pass an existing gate without changing a tool, stop and report
the smallest conflict. M0 is not the place to redesign a tool.

### The two tool files M0 did touch, and why

Both were forced by the transition, both were reported before being made, and neither changes what any gate
checks. They are listed here rather than left for a reviewer to find in a diff.

- **`tools/claim-matrix-gate.py` — `TSV_REL` and `REQUIRED_OBLIGATIONS`.** The gate pinned the repair-21
  matrix as its subject, so M0-05 could not retire that file: `make claims` failed with *"claim matrix: …
  does not exist"*. Worse, once M0 changed the M-series wording and the human act — which it is required to
  do — the retained matrix's own evidence tokens dangled, so `make claims` failed either way. There was no
  state in which the matrix survived M0 and the gate passed. Reported as the smallest M0 conflict; **Rob
  authorized the retarget on 2026-07-27**. The gate's behaviour is unchanged — only its subject moved, as it
  has at every checkpoint boundary. One consequence came with it: the builder-prohibition control needs a row
  carrying a Rocq surface, and a documentation checkpoint has none, so that control now appends its own
  subject instead of degrading into *"could not construct the scenario"*.
- **`tools/human-review-index.py` — the `BANNER` string.** The accepted-amendment line and the C4 status line
  live inside the generator because D-07 forbids hand-editing the generated view. Adding A008 and recording
  C4 as accepted therefore cannot be done anywhere else. Five lines of text; no logic.

No other tool, and no Rocq, OCaml, Go, container, Make, Dune, hook, fixture or golden file, differs from the
accepted C4 candidate.

## 3. Obligations

`.review/M0_OBLIGATION_MATRIX.tsv` holds one row per obligation, with these stable IDs.

| ID | Obligation |
|---|---|
| `M0-01` | Publish the exact C4 human acceptance and remove the C4 review from open human acts. |
| `M0-02` | Install A008, D-28 and D-29 in every affected FCB authority. |
| `M0-03` | Make M0 the sole current work and insert it before M1 everywhere. |
| `M0-04` | Add one durable M0 review human act with no candidate identity or repair history. |
| `M0-05` | Retire the active C4 repair-21 documents and candidate archaeology to Git history. |
| `M0-06` | Accept the recorded default of the open implementation question, delete it, and leave none open. |
| `M0-07` | Regenerate the Human Review Index and keep D-07 exact in both directions. |
| `M0-08` | Keep the D-24 manifest, live-set table, owner markers and authority scan exact. |
| `M0-09` | Preserve every C4 proof, artifact and generated byte without functional changes. |
| `M0-10` | Freeze one M0 candidate and request a separate human M0 review. |

## 4. Exact current-state ownership

After the M0 implementation candidate is frozen:

- `.review/NEXT_STEPS.md` alone owns the exact M0 candidate and mutable status;
- the canonical human-acts data owns the durable M0 review act only, worded as *"Review the exact M0 candidate
  named by `.review/NEXT_STEPS.md`, and accept or block it. Only Rob accepts M0."*;
- the generated Human Review Index contains no candidate identity;
- the Roadmap owns stable order only;
- Git history owns the blocked C4 candidates and the repair narratives;
- no live authority calls repair 21 active or C4 unaccepted;
- no live authority lists the twenty-two blocked C4 candidates.

## 5. Scope assignments that must remain visible

These findings are real and are NOT M0 work. Recording them here is the mandatory follow-up assignment D-28
requires; it is not permission to ignore them, and it is not permission to do them now.

| Finding class | Mandatory owner |
|---|---|
| obsolete or long source comments, archaeology, prose volume, dead source files and declarations | M1 Source Diet |
| cold, warm, no-op and incremental timing; dependency graph; edit frequency; cache behaviour | M2 Build Observatory |
| auxiliary tools, host and container placement, build-graph factoring, duplicate and weaker gates | M3 Tool and Build Architecture Audit |
| file splitting, proof partitioning, cache and build-graph restructuring | M4 Mechanical Refactor, after Rob approves the plan |

The open showroom, external-evidence, ADR-0002, ADR-0004 and M4-plan human acts keep their existing owners. M0
does not absorb them: no current contract names M0 as their dependency.

## 6. Verification

Run the existing checks against the working tree and the exact staged export where supported: the live-FCB
document gates, the claim matrix, the scoped-name gate, the full check, regeneration, the publication-edge
guard, the format report, and the real pre-commit hook on the staged snapshot without bypassing it.

Also prove:

- no file this contract forbids changed between the accepted C4 candidate and the M0 implementation candidate;
- the generated module bytes are identical to the accepted C4 bytes;
- the executable still matches the reviewed stdout, stderr and exit status;
- the whole-theory assumption result is unchanged;
- the accepted C4 ref is named exactly in the historical acceptance record, not as current work.

There is deliberately no prose-heavy M0 closure audit. This contract, the matrix, the generated views, the
gate outputs, the commit messages and the Git diff are the evidence.

## 7. Exit condition

M0 is ready for review only when all ten obligation rows are closed with exact evidence; C4 is stated as
accepted everywhere current status appears; M0 is the sole active work; M1 is next but forbidden until M0 is
accepted; no active C4 repair document or candidate history remains in the live tree; no functional file
changed; every required check passes; the review request names one exact M0 implementation candidate; and a
documentation-only freeze names that candidate without claiming human acceptance.

Only Rob accepts M0.
